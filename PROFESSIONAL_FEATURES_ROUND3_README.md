# تدقيق شامل + جولة ثالثة من المزايا الاحترافية

طُلب مراجعة كاملة للكود لتحديد ثغرات حقيقية أولاً، ثم اقتراح مزايا جديدة
تُحدث فرقاً حقيقياً في تجربة النظام الاحترافية. نُفِّذ الاثنان بالكامل.

---

## القسم أ: 8 إصلاحات من التدقيق الأمني/المحاسبي

| # | الثغرة | الإصلاح |
|---|---|---|
| 1 | تقييم صفري يفشل عند استلام قطعة دوارة تالفة لأول مرة | `allow_zero_valuation_rate=1` على سطر Stock Entry نفسه (الحقل يعيش هناك، وليس على بطاقة الصنف) |
| 2 | فشل إلغاء Stock Entry يُسقِط إلغاء أمر العمل بالكامل | try/except + تنبيه للمالية لتسوية يدوية |
| 3 | شطب الأصل يتجاهل تعدد دفاتر الإهلاك | تمرير `finance_book` صراحة بدل finance_books[0] الضمني |
| 4 | استبدال عداد فيزيائي يوقف محرك الصيانة بالعداد نهائياً | إعادة ضبط المرجع لـ 0 عند رصد هبوط في القراءة |
| 5 | قراءات ثابتة متكررة (سلسلة تبريد) تُصنَّف تكراراً زائفاً | `reading_date` أصبح Datetime بدل Date فقط |
| 6 | مهمة صيانة مُغطاة بمستوى أعلى تبقى "متأخرة" للأبد | تقديم `next_due_date` تلقائياً للدورة القادمة |
| 7 | فني يقدر يوقّع على عزل نفَّذه هو بنفسه (LOTO) | دور "Safety Inspector" مطلوب للتوقيع |
| 8 | فني يقدر يُغلق أمر عمل مُسنَد لزميل آخر عبر الموبايل | تقييد `update_job_status` بالفني المُسنَد فعلياً |

---

## القسم ب: 10 مزايا احترافية جديدة (Round 3)

### 1. تفعيل مصفوفة الحرجية (Asset Criticality Matrix)
كانت DocType بلا أي أثر تشغيلي. الآن: أصل High/Critical يرفع أولوية
أوامر عمله تلقائياً، ويحدّ أقصى فاصل بين فحوصات السلامة (30/90 يوماً).
مهمة جديدة `check_safety_inspection_due` (لم تكن موجودة إطلاقاً).

### 2. تقييم أداء المورّد التلقائي
زر "احسب من بيانات أوامر العمل الفعلية" يملأ سرعة الاستجابة والالتزام
بالمواعيد من بيانات Asset Work Order الحقيقية (SLA breach، زمن الاستجابة
الفعلي) بدل تقييم ذاتي بحت. باقي المعايير (جودة/تسعير/تواصل) تبقى يدوية
عمداً — لا مقياس بيانات فعلي لها.

### 3. نسبة التزام الصيانة الوقائية في مؤشر صحة الفرع
بند خامس جديد في Branch Health Score: نسبة مهام Asset Maintenance Task
المتأخرة لأصول كل فرع.

### 4. كشف انحراف إحصائي (SPC) في قراءات الحرارة
قاعدتان كلاسيكيتان (3-Sigma + اتجاه مستمر لـ 5 قراءات) تكتشفان تدهوراً
تدريجياً في التبريد قبل تجاوز الحد الآمن فعلياً — مكمِّلة (وليست بديلة)
لتنبيه الخرق الفعلي الموجود.

### 5. معالج فتح فرع جديد
ينشئ Cost Center + Warehouse + Location الافتراضية دفعة واحدة (أو يربط
بموجود منها)، ثم الفرع نفسه مربوطاً بها — بدل 4 شاشات منفصلة.

### 6. تنبيه تجاوز الميزانية الرأسمالية
مهمة أسبوعية جديدة تنبّه Asset Finance Manager عند اقتراب (80%) أو
تجاوز (100%+) الإنفاق الفعلي للميزانية المعتمدة.

### 7. نقطة إعادة طلب ديناميكية لقطع الغيار
تُحسَب من معدل الاستهلاك الفعلي (آخر 180 يوماً) × (مهلة توريد + مخزون
أمان) بدل رقم ثابت. `minimum_qty` اليدوي يبقى أرضية دنيا لا تُخفَّض أبداً.

### 8. ملخص يومي موحّد (اختياري)
بريد واحد يومياً لكل مستخدم يجمع كل تنبيهاته من اليوم — بدل 24 إشعاراً
متناثراً. معطَّل افتراضياً (Asset Mgmt Settings)، إضافي بالكامل (لا يمسّ
إشعارات الجرس).

### 9. لوحة أصل 360°
شاشة واحدة تلخّص حالة الأصل *الآن*: AHI، RUL، TCO، الحرجية، أوامر العمل
المفتوحة، الضمان، آخر فحص سلامة — مكمِّلة لسجل التدقيق التاريخي.

### 10. طباعة دفعية لملصقات الباركود
فلاتر (فرع/فئة/غير المُرمَّزة فقط) ثم دمج كل الملصقات المطابقة في PDF
واحد عبر آلية Frappe الأصلية للطباعة الدفعية — بلا كود توليد PDF جديد.

---

## بعد التنفيذ (على السيرفر الفعلي)

```
git pull && bench migrate && bench clear-cache && bench build && bench restart
```

**يتطلب إجراء يدوي واحد:** إسناد دور "Safety Inspector" (يُنشأ تلقائياً
عند الـ migrate) لموظف السلامة المناسب — بدونه، لا يمكن لأي أحد التوقيع
على قوائم عزل الطاقة (LOTO) حتى يُسنَد الدور لشخص واحد على الأقل.

كل الاختصارات الجديدة مرتبطة في workspace "إدارة الأصول" — لا حاجة لأي
إعداد يدوي آخر.
