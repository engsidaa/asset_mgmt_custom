# صفحات الويزارد (خطوة بخطوة) — التوثيق الكامل

توثيق دقيق لمرحلة "wizard-ification": 4 صفحات ويزارد مستقلة (Desk Page
كامل، وليس Dialog) تحوِّل النماذج الأكثر استخداماً إلى تجربة إرشادية
خطوة-بخطوة، بدل نموذج طويل واحد — تجهيزاً للربط لاحقاً بتطبيق موبايل
لمدير الفرع. كل صفحة هنا مبنية على نفس الإطار المشترك، وتُنفِّذ العمل
الفعلي عبر الدوال المُوثَّقة والمُحمِيّة (whitelisted) الموجودة أصلاً في
النظام — لا يوجد منطق أعمال مكرَّر داخل JavaScript.

**تم التحقق الساكن فقط** (syntax صحيح لكل ملف عبر `node --check` و
`python3 -m py_compile`) — لا يوجد site متصل في بيئة التنفيذ، فخطوات
الاختبار اليدوي أسفل كل صفحة لازم تُنفَّذ بواسطتك بعد `bench migrate`.

---

## الإطار المشترك (Wizard Framework)

- `public/js/wizard_framework.js` (`asset_mgmt_custom.Wizard`) + `public/css/wizard_framework.css`
  محمَّلان تلقائياً في كل صفحات Desk (`app_include_js`/`app_include_css`
  في `hooks.py`)، فأي صفحة ويزارد جديدة مستقبلاً تقدر تستخدمهم مباشرة.
- كل خطوة: `{id, label, render($body, wizard), validate(wizard), on_leave(wizard)}`.
  `wizard.make_field()` يبني حقول Frappe القياسية (Link/Select/Currency...)
  خارج نموذج مربوط، و`wizard.state` يحمل كل القيم عبر الخطوات.
  `finish(wizard)` تُنفَّذ عند تأكيد آخر خطوة.
- `asset_mgmt_custom.Wizard.upload_file(file, {doctype, docname, fieldname})`:
  رفع ملف عبر `/api/method/upload_file` مباشرة (بدون واجهة رفع منبثقة).
  **مهم:** هذا الاستدعاء يُنشئ سجل File مرتبطاً بالمستند فقط — **لا يضبط
  قيمة حقل Attach نفسه تلقائياً**. أي صفحة تحتاج ربط الملف بحقل فعلي على
  المستند (وليس مجرد مرفق عام) لازم تستدعي `frappe.client.set_value`
  بعدها بـ `file_url` من نتيجة الرفع — تم اكتشاف هذا أثناء بناء صفحة
  الترميز، وتبيَّن أنه كان يسبب نفس الخلل في صفحة "طلب صيانة جديد" (حقل
  `fault_photo` كان يُترك فارغاً بصمت رغم رفع الصورة فعلياً) — **تم
  إصلاحه في نفس الدفعة**.

---

## 1) طلب صيانة جديد — `/app/asset-work-order-wizard`

**الأدوار:** System Manager, Asset Manager, Branch Manager, Employee.
4 خطوات: اختيار الأصل (مع عرض تفاصيله) ← نوع العمل والأولوية ← وصف
العطل + صورة اختيارية ← المراجعة والإرسال. الإرسال يستدعي
`asset_mgmt_custom.api.branch_manager.create_maintenance_request` (نفس
الدالة المستخدمة في الـ API المتوافق مع الموبايل)، ثم يرفع الصورة ويربطها
فعلياً بحقل `fault_photo`.

**اختبار:** افتح الصفحة ← اختر أصلاً ← أكمل الخطوات ← أرفق صورة عطل ←
أرسل ← تأكد من فتح "Asset Work Order" الناتج وأن `fault_photo` يعرض
الصورة فعلياً (وليس فارغاً).

## 2) طلب أصل جديد — `/app/asset-requisition-wizard`

**الأدوار:** System Manager, Asset Manager, Branch Manager, Employee.
4 خطوات: الفئة والكمية والفرع ← عرض الطلبات/الأصول المعلَّقة لنفس الفئة
(تجنباً للتكرار، عبر `get_new_requisition_context`) ← الوصف والمبرر
والتاريخ المطلوب ← المراجعة. الإرسال يستخدم
`frappe.client.insert` ثم `frappe.client.submit` بالمستند الكامل
المُعاد من `insert` (وليس dict جزئي — نمط `frm.save()`→`frm.savesubmit()`
نفسه)، فيبدأ الطلب سلسلة الاعتماد الثلاثية فوراً (المالية ← مدير الفرع
← إدارة الأصول).

**اختبار:** أرسل طلباً ← تأكد أنه submitted (docstatus=1) وليس مسودة ←
تأكد أنه يظهر في قائمة "طلبات معلَّقة" عند فتح الويزارد مرة أخرى لنفس
الفئة.

## 3) إتمام / رفض أمر عمل — `/app/work-order-completion-wizard`

**الأدوار:** System Manager, Asset Manager, Asset Technician, Maintenance
Vendor. 4 خطوات: اختيار أمر العمل (مفلتر على الحالات المفتوحة فقط) ←
اختيار إتمام أو رفض ← التفاصيل (تكلفة فعلية + ملاحظات + صورة اختيارية،
أو سبب الرفض) ← المراجعة والتأكيد. الإرسال يستدعي `run_doc_method` على
`complete_work_order()`/`reject_work_order()` الموجودتين أصلاً في
`asset_work_order.py` — وليس منطقاً مكرَّراً — فكل الحماية الحالية
(`_check_maintenance_role`: فقط الطاقم أو المورِّد المُسنَد له تحديداً)
والترحيل المحاسبي عبر `self.save()` (`on_update_after_submit`) يعملان
تماماً كما لو ضُغط الزر من النموذج العادي.

**اختبار:**
- بحساب "Maintenance Vendor" مُسنَد لأمر عمل: تأكد أن القائمة في الخطوة
  1 تعرض فقط أوامر العمل المُسنَدة له.
- أكمل أمراً بتكلفة فعلية > 0 ← تأكد من ظهور قيد محاسبي (GL Entry)
  مطابق تماماً لما يحدث عند الإتمام اليدوي.
- ارفض أمراً بسبب ← تأكد أن الحالة "مرفوض" نهائية ولا تظهر أزرار أخرى.

## 4) ترميز / تفعيل أصل — `/app/asset-coding-wizard`

**الأدوار:** System Manager, Asset Manager, Branch Manager. صفحة جديدة
تجمع `mark_coded()` و`set_operational()` (الموجودتين في `overrides/asset.py`)
في تدفّق واحد بدل إجراءين منفصلين على النموذج:

1. اختيار الأصل (مفلتر على غير المُفعَّل تشغيلياً بعد) — يعرض حالة
   الترميز والتشغيل الحاليتين.
2. الترميز: نوع الترميز (Barcode/RFID/Iron Code) + الكود المطابق + صورتا
   قبل/بعد اللصق — **تُخطَّى تلقائياً برسالة توضيحية** لو الأصل مُرمَّز
   بالفعل (بدل تكرار طلب بيانات لا داعي لها).
3. تفعيل تشغيلي اختياري فوري (checkbox) — أو تأجيله لاحقاً.
4. المراجعة والتأكيد.

عند التأكيد: يضبط حقول الترميز، يرفع الصورتين **ويربطهما فعلياً**
بحقلي `custom_tagging_photo_before`/`custom_tagging_photo` (عبر
`frappe.client.set_value` بعد الرفع — بالضبط النمط الذي كان ناقصاً في
صفحة طلب الصيانة قبل الإصلاح)، ثم يستدعي `mark_coded()`، وأخيراً
`set_operational()` إن اختير التفعيل الفوري — بنفس شروط النموذج العادي
تماماً (رفض التفعيل قبل الترميز، رفض الترميز بلا الصورتين).

**اختبار:**
- اختر أصلاً "Uncoded" ← أكمل الترميز بدون إحدى الصورتين ← تأكد من ظهور
  رسالة خطأ ومنع المتابعة (نفس رسالة `mark_coded` بالضبط).
- أكمل الترميز بالكامل + فعِّل تشغيلياً فوراً ← افتح نموذج Asset وتأكد
  أن `custom_tagging_photo_before`/`custom_tagging_photo` يعرضان
  الصورتين فعلياً (وليس فارغين)، وأن `available_for_use_date` أصبح
  اليوم، وأن الإهلاك بدأ يُحتسب.
- اختر أصلاً "Coded" (لكن غير مُفعَّل) ← تأكد أن خطوة الترميز تُخطَّى
  تلقائياً برسالة "مُرمَّز بالفعل" ولا تُطلب بيانات ترميز جديدة.

---

## الاختصارات في الواجهات (Workspaces)

أُضيف قسم "الويزارد الإرشادي (خطوة بخطوة)" في كل من:
- **الصيانة**: اختصارا "طلب صيانة (ويزارد)" و"إتمام/رفض أمر عمل (ويزارد)".
- **إدارة الأصول**: اختصارا "طلب أصل (ويزارد)" و"ترميز/تفعيل أصل (ويزارد)".

هذه اختصارات إضافية بجانب النماذج الكاملة الأصلية (لم تُحذف أو تُستبدل) —
أي مستخدم يقدر يختار الطريقة المناسبة له.

**اختبار:** افتح Workspace "الصيانة" و"إدارة الأصول" ← تأكد من ظهور قسم
"الويزارد الإرشادي" في كل منهما مع الاختصارات الأربعة تعمل وتفتح الصفحات
الصحيحة.

---

## ملاحظة توافق الموبايل

كل صفحة ويزارد هنا تستدعي فقط دوال `@frappe.whitelist()` موجودة أصلاً
(`branch_manager.*`, `frappe.client.*`, `run_doc_method`, `mark_coded`,
`set_operational`) — نفس الدوال القابلة للاستدعاء من أي عميل REST خارجي
(بما فيه تطبيق موبايل مستقبلي). منطق الويزارد نفسه (الخطوات، الفحص،
التخطي الشرطي) موجود فقط في JavaScript هذه الصفحات تحديداً، فتطبيق
الموبايل هيحتاج يعيد بناء نفس تسلسل الخطوات على الواجهة الخاصة به —
لكن كل استدعاء API خلفه جاهز ومُختبَر منطقياً بالفعل.
