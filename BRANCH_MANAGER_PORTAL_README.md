# بوابة مدير الفرع الذاتية (Branch Manager Self-Service Portal)

توثيق دقيق لكل ما تم فحصه وتنفيذه لبناء بوابة يرى فيها مدير كل فرع/مطعم
أصول فرعه فقط، يقدر يطلب أصولاً جديدة، يتابع صيانتها الدورية، وينشئ
طلبات صيانة (بوصف وصورة للعطل) ويتابع تعليقاتها — مع طبقة API منفصلة
مُصمَّمة من الأساس لتُستهلَك من **تطبيق موبايل لاحقاً**، وليس فقط من
واجهة الديسك.

**تنبيه صريح حول حدود الاختبار هنا:** لا يوجد في بيئة التنفيذ هذه موقع
Frappe فعلي متصل (site) — كل ما يلي تم التحقق منه **سكونياً** (syntax
صحيح لكل JSON/Python/JS، تتبّع منطقي دقيق لكل مسار تنفيذ، ومطابقة كل اسم
حقل/دالة إطار عمل استُخدم مقابل الكود المصدري الفعلي لـ Frappe/ERPNext
— ليس افتراضاً). **لم يُشغَّل `bench migrate` ولا أي اختبار تكاملي فعلي
من هنا** — خطوات الاختبار اليدوي أسفل هذا الملف لازم تُنفَّذ بواسطتك على
السيرفر الفعلي بعد الترحيل.

---

## جدول المحتويات

1. [الفكرة الأساسية: كيف يعمل التقييد الجغرافي](#الفكرة-الأساسية-كيف-يعمل-التقييد-الجغرافي)
2. [كل ما تم إضافته بالتفصيل](#كل-ما-تم-إضافته-بالتفصيل)
3. [مرجع API الكامل لتطبيق الموبايل](#مرجع-api-الكامل-لتطبيق-الموبايل)
4. [خطوات الترحيل المطلوبة](#خطوات-الترحيل-المطلوبة)
5. [بروتوكول الاختبار اليدوي الكامل](#بروتوكول-الاختبار-اليدوي-الكامل)
6. [قرارات صُممت عمداً بهذا الشكل](#قرارات-صُممت-عمداً-بهذا-الشكل)
7. [حدود ما تم تنفيذه](#حدود-ما-تم-تنفيذه)

---

## الفكرة الأساسية: كيف يعمل التقييد الجغرافي

**لا يوجد أي فلتر يدوي مكتوب في أي كود يقول "أظهر فقط أصول هذا الفرع".**
التقييد بالكامل مبني على آلية أصلية جاهزة في Frappe اسمها **User
Permission**:

1. لكل مستخدم مُعيَّن كـ `Branch.custom_branch_manager` لفرع معين، يُنشأ
   تلقائياً سجل `User Permission` (`allow="Branch"`,
   `for_value=<اسم الفرع>`, `apply_to_all_doctypes=1`).
2. أي DocType فيه حقل Link مباشر لـ Branch (مثل `Asset.custom_branch`،
   `Asset Work Order.branch`، `Asset Requisition.branch`) — Frappe
   يُصفِّي تلقائياً أي قائمة/تقرير يفتحه هذا المستخدم، فلا يظهر له إلا
   صفوف فرعه فقط. **هذا سلوك أصلي في Frappe نفسه، مش كود كتبناه.**
3. المشكلة الوحيدة: `Asset Maintenance` (خطة الصيانة الدورية) كان معندهاش
   حقل Link مباشر لـ Branch أصلاً (بس `asset_name` → Link لـ Asset) —
   فأُضيف حقل `custom_branch` (Link→Branch، مُجلَب تلقائياً من
   `asset_name.custom_branch`) خصيصاً عشان يفعل نفس التصفية التلقائية
   عليها.
4. **تحذير برمجي دقيق تم الانتباه له في كل كود الـ API الجديد:**
   `frappe.get_all()` في Frappe يضبط `ignore_permissions=True` دائماً
   (يتجاوز User Permission بالكامل)، بينما `frappe.get_list()` فقط هو
   اللي يُطبِّقها. كل استعلام في `asset_mgmt_custom/api/branch_manager.py`
   يستخدم `get_list` حصراً لهذا السبب بالتحديد.

هذا يعني: بمجرد ضبط `custom_branch_manager` على أي فرع، كل شيء (قوائم
الديسك، تقارير، الـ API الجديد) يُصفَّى تلقائياً بدون أي كود إضافي مستقبلي
— أي DocType جديد تضيفه لاحقاً وفيه Link مباشر لـ Branch سيُصفَّى
تلقائياً بنفس الطريقة.

---

## كل ما تم إضافته بالتفصيل

### 1. الدور الجديد "Branch Manager"

لم يُنشأ يدوياً كسجل `Role` منفصل — بل يُنشأ تلقائياً بواسطة Frappe نفسه
أول مرة يُشغَّل فيها `bench migrate` (لأنه مُشار إليه الآن داخل
`permissions` الخاصة بـ `Asset Requisition` و`Asset Work Order`، ونفس
آلية مزامنة الـ DocType تُنشئ أي Role مفقود تلقائياً — نفس ما حدث سابقاً
مع كل أدوار هذا التطبيق الأخرى مثل "Asset Manager").

### 2. Custom DocPerm لصلاحيات على DocTypes أساسية (Core)

`Asset` و`Asset Maintenance` مستندان أساسيان في ERPNext — **لا يمكن
تعديل ملفاتهما JSON مباشرة**. الطريقة المدعومة رسمياً لإضافة صلاحية دور
جديد عليهما هي `Custom DocPerm` (نفس مفهوم Custom Field لكن للصلاحيات).
أُضيف ملف جديد `fixtures/custom_docperm.json`:

| DocType | الدور | الصلاحيات |
|---------|------|-----------|
| Asset | Branch Manager | read, report, print, email فقط (لا write/create/delete) |
| Asset Maintenance | Branch Manager | read, report, print فقط |

### 3. حقل جديد: `Asset Maintenance.custom_branch`

Link→Branch، `fetch_from: "asset_name.custom_branch"`, للقراءة فقط —
سبب وجوده مشروح بالتفصيل في القسم السابق (شرط ضروري لتفعيل التصفية
التلقائية).

### 4. `overrides/branch.py` — مزامنة تلقائية لصلاحية الرؤية

هوك جديد `on_update` على `Branch` (مُسجَّل في `hooks.py`) يستدعي
`sync_branch_manager_permission(branch_name)` في كل مرة يُحفَظ فيها أي
فرع:

- لو `custom_branch_manager` اتغيَّر أو اتحدد لأول مرة: يُنشئ `User
  Permission` جديدة له (لو مش موجودة)، ويُضيف له دور "Branch Manager"
  تلقائياً (عبر `User.add_roles()`) لو مكانش عنده أصلاً.
- لو مدير فرع سابق تمت إزالته أو استبداله: تُحذَف صلاحية الرؤية الخاصة
  به لهذا الفرع تحديداً فقط (لا تمسّ صلاحياته على فروع أخرى يديرها إن
  وُجدت).
- **الدور نفسه لا يُزال تلقائياً أبداً** — إزالته قرار إداري يدوي فقط
  (تفادياً لأي إجراء صلاحيات هدّام تلقائي).

### 5. `setup/after_migrate.sync_branch_manager_user_permissions()`

دالة إعادة-مزامنة شاملة (idempotent، آمنة للتشغيل في كل `bench migrate`)
تمر على كل الفروع وتُطبِّق نفس منطق الهوك أعلاه — تغطي أي حالة تغيَّر
فيها `custom_branch_manager` بطريقة لم تمرّ عبر `Document.save()` العادي
(استيراد بيانات مباشر مثلاً).

### 6. صلاحيات "Branch Manager" على مستندات هذا التطبيق نفسه

| DocType | read | write | create | submit | ملاحظات |
|---------|------|-------|--------|--------|----------|
| Asset Requisition | ✓ | ✓ | ✓ | ✓ | يقدر يطلب أصولاً جديدة لفرعه ويُسلِّم الطلب بنفسه |
| Asset Work Order | ✓ | ✓ | ✓ | ✓ | يقدر ينشئ طلب صيانة ويُسلِّمه — **لا يقدر** يُتمّه أو يرفضه (محجوز لفريق الصيانة، انظر التالي) |

### 7. Asset Work Order — حالة "مرفوض" + صورة العطل + مسارات مُصرَّح بها

- خيار جديد في `status`: **"مرفوض"** (إضافة إلى: مفتوح، قيد التنفيذ،
  مكتمل، معلق، ملغي).
- حقول جديدة: `rejection_reason` (Small Text)، `rejected_by`
  (Link→User)، `rejected_on` (Datetime) — تُملأ تلقائياً عند الرفض.
- حقل جديد: `fault_photo` (Attach Image) — لتوثيق صورة العطل وقت إنشاء
  طلب الصيانة.
- قسم جديد "الرفض" في النموذج، يظهر فقط لو `status == 'مرفوض'`.
- طريقتان مُصرَّح بهما (`@frappe.whitelist()`) جديدتان في
  `asset_work_order.py`، **تحلّان محل** الزر القديم اللي كان يستخدم
  `frappe.client.set_value` مباشرة:
  - `complete_work_order()` — يضبط الحالة "مكتمل"، ويمرّ عبر
    `self.save()` الكامل (وليس `db_set`) عمداً حتى يُشغِّل
    `on_update_after_submit` بشكل طبيعي — وهو نفسه المسؤول عن ترحيل
    القيد المحاسبي لتكلفة الصيانة (من عمل سابق على هذا التطبيق). **هذا
    يعني أن حسابات الصيانة تبقى مترابطة بالضبط كما كانت من قبل**، بغض
    النظر عمن استدعى الإتمام (زر الواجهة، أو تطبيق الموبايل لاحقاً عبر
    نفس المسار).
  - `reject_work_order(reason)` — رفض نهائي بسبب إجباري، بدون خطوة
    "إعادة تقديم" (بناءً على قرارك الصريح)، ولا يُنشئ أي قيد محاسبي (فقط
    الإتمام يُنشئ قيداً).
  - كلتاهما محميتان بفحص `_check_maintenance_role()`: فقط "Asset
    Technician" أو "Asset Manager" أو "System Manager" يقدروا يستدعوهما
    — رغم إن Branch Manager عنده `write` على المستند (لتحرير مسودته قبل
    التسليم)، **لا يقدر يُتمّ أو يرفض طلبه هو نفسه**.
  - أزرار الواجهة (`asset_work_order.js`) اتحدَّثت لاستدعاء هاتين
    الطريقتين بدل التعديل المباشر، وأصبحت لا تظهر أصلاً إلا للمستخدم اللي
    عنده دور صيانة فعلي (فحص `frappe.user_roles` في الواجهة، مطابق تماماً
    لفحص السيرفر).

### 8. `asset_mgmt_custom/api/branch_manager.py` — طبقة API للموبايل

ملف جديد كامل، مُفصَّل في القسم التالي.

---

## مرجع API الكامل لتطبيق الموبايل

كل دالة هنا `@frappe.whitelist()`، تُستدعى عبر:
`POST /api/method/asset_mgmt_custom.api.branch_manager.<اسم الدالة>`
(أو كـ Document method عبر `run_method` لدوال `Asset Work Order` نفسها،
انظر أدناه).

| الدالة | المدخلات | ماذا تُرجع | ملاحظة |
|--------|----------|-----------|--------|
| `get_dashboard_summary()` | — | عدّادات: إجمالي الأصول، طلبات معلَّقة، أوامر عمل مفتوحة، أصول Incomplete/In Transit، بنود صيانة قادمة خلال 30 يوماً | شاشة رئيسية واحدة بدل 5 استدعاءات |
| `list_my_assets(asset_category=None)` | فئة أصل اختيارية | قائمة أصول مختصرة (اسم، فئة، صورة، حالة، ضمان، الصيانة القادمة) | لعرض بطاقات في شاشة القائمة |
| `get_asset_detail(asset)` | اسم الأصل | حزمة كاملة: البيانات المالية، الضمان، أوامر العمل المفتوحة، بنود الصيانة الدورية، الملفات المرفقة، التعليقات | يتحقق أولاً من صلاحية القراءة على هذا الأصل تحديداً |
| `get_new_requisition_context(asset_category=None)` | فئة اختيارية | طلبات معلَّقة لفرعه + أصول قيد التفعيل/النقل | يُستدعى قبل شاشة "طلب أصل جديد" |
| `create_maintenance_request(asset, problem_description, work_type=None, priority=None)` | اسم الأصل + وصف العطل (إجباري) + نوع/أولوية اختياريان | `{name, status}` لأمر العمل الذي أُنشئ **وسُلِّم فوراً** | صورة العطل تُرفَع بعدها منفصلة (انظر أدناه) |
| `list_my_work_orders(status=None)` | حالة اختيارية | قائمة أوامر عمل الفرع | |
| `add_reminder(reference_doctype, reference_name, reminder_date, note=None)` | نوع مستند (Asset / Asset Requisition / Asset Work Order فقط) + اسمه + تاريخ | `{name}` لسجل ToDo الذي أُنشئ | تذكيرات = ToDo قياسي في Frappe، وليس مفهوماً جديداً |

### رفع صورة العطل (fault_photo)

لا توجد دالة مخصصة لرفع الصور — يُستخدَم مسار Frappe القياسي لرفع
الملفات، بعد الحصول على اسم أمر العمل من `create_maintenance_request`:

```
POST /api/method/upload_file
  file: <binary>
  doctype: "Asset Work Order"
  docname: "<الاسم المُرجَع من create_maintenance_request>"
  fieldname: "fault_photo"
  is_private: 0  (أو 1 حسب سياسة الخصوصية المطلوبة)
```

### إتمام/رفض طلب الصيانة (لفريق الصيانة، وليس مدير الفرع)

هاتان طريقتان على المستند نفسه (`Asset Work Order`)، وليستا في وحدة
`api/branch_manager.py` — تُستدعيان عبر:

```
POST /api/resource/Asset Work Order/<name>?run_method=complete_work_order
POST /api/resource/Asset Work Order/<name>?run_method=reject_work_order
  reason: "<سبب الرفض>"
```

### التعليقات (على أي مستند: أصل، أمر عمل، طلب أصل)

لا حاجة لأي دالة مخصصة — Frappe نفسه يوفر endpoints قياسية للتعليقات
(قراءة عبر `get_asset_detail` أعلاه للأصل، أو
`GET /api/resource/Comment?filters=[["reference_doctype","=","Asset Work Order"],["reference_name","=","<name>"]]`
لأي مستند آخر)، والإضافة عبر
`frappe.desk.form.utils.add_comment` القياسية — كل ما يلزم هو أن يملك
المستخدم صلاحية `read` على المستند المرجعي، وهو ما توفره صلاحيات "Branch
Manager" أعلاه تلقائياً.

---

## خطوات الترحيل المطلوبة

```bash
cd ~/frappe-bench/apps/asset_mgmt_custom
git pull origin claude/asl-aliye-amal-pmmeul
cd ~/frappe-bench
bench migrate      # يُنشئ الدور الجديد + الحقول الجديدة + Custom DocPerm
                    # + يُشغِّل sync_branch_manager_user_permissions تلقائياً
bench restart
```

بعد الترحيل، الخطوة الوحيدة المطلوبة يدوياً: **تأكد أن `Branch.custom_branch_manager`
مضبوط فعلاً على كل فرع تريد لمديره صلاحية هذه البوابة** — أي فرع بدون
مدير محدد، لن يحصل أي مستخدم على أي صلاحية جديدة له تلقائياً (لا ضرر،
لكن لا فائدة أيضاً حتى يُضبط الحقل).

---

## بروتوكول الاختبار اليدوي الكامل

### 1. التحقق من إنشاء الدور والصلاحيات

1. افتح **Role List** بعد `bench migrate` — تأكد من وجود "Branch Manager".
2. افتح **Role Permission Manager** ← اختر Asset ← تأكد أن "Branch
   Manager" ظاهر بصلاحية read فقط.

### 2. التحقق من المزامنة التلقائية

1. افتح **Branch** لفرع تجريبي ← اضبط `custom_branch_manager` = مستخدم
   حقيقي (غير System Manager) ← احفظ.
2. افتح **User Permission List** ← تأكد من وجود سجل جديد
   (User = هذا المستخدم، Allow = Branch، For Value = هذا الفرع).
3. افتح **User** لهذا المستخدم ← تبويب Roles ← تأكد أن "Branch Manager"
   أُضيف تلقائياً.
4. سجّل دخول بهذا المستخدم (أو استخدم "Login As" من قائمة المستخدم إن
   كنت System Manager) ← افتح قائمة **Asset** ← تأكد أنه **لا يرى إلا**
   أصول هذا الفرع (وليس أصول أي فرع آخر).
5. افتح قائمة **Asset Maintenance** بنفس المستخدم ← تأكد من نفس التصفية.
6. غيِّر `custom_branch_manager` لمستخدم آخر على نفس الفرع ← تأكد أن
   `User Permission` الخاصة بالمستخدم الأول اختفت، وظهرت للثاني.

### 3. طلب أصل جديد (Asset Requisition) من مدير الفرع

1. بنفس مستخدم مدير الفرع، أنشئ **Asset Requisition** جديداً لفرعه ←
   اعتمد (Submit) — تأكد أنه ينجح (كان هذا مستحيلاً قبل هذا التغيير بدون
   دور "Employee").
2. تأكد أن الحالة أصبحت "Pending Finance Approval" تلقائياً.

### 4. طلب صيانة (Asset Work Order) من مدير الفرع

1. بنفس المستخدم، أنشئ **Asset Work Order** جديداً لأصل من فرعه، اكتب
   `problem_description`، ارفع صورة في `fault_photo`، اعتمد.
2. تأكد أن الحالة أصبحت "قيد التنفيذ" تلقائياً (`on_submit`).
3. تأكد أن **زرَّي** "إتمام أمر العمل" و"رفض الطلب" **لا يظهران** لهذا
   المستخدم (فحص الدور في الواجهة).
4. سجّل دخول بمستخدم له دور "Asset Technician" ← افتح نفس الأمر ← تأكد
   أن الزرين يظهران الآن.
5. اضغط "رفض الطلب" ← اكتب سبباً ← تأكد أن الحالة أصبحت "مرفوض"، وأن
   `rejection_reason`/`rejected_by`/`rejected_on` امتلأت.
6. أنشئ أمر عمل آخر بتكلفة فعلية (`actual_cost`) واضغط "إتمام أمر العمل"
   ← تأكد أن قيد اليومية (Journal Entry) اتنشأ بنفس الطريقة المُوثَّقة في
   `MAINTENANCE_MANAGEMENT_README.md` — **هذا يتحقق تحديداً من أن حسابات
   الصيانة ما زالت مترابطة مع النظام بعد كل هذا التعديل**.

### 5. اختبار الـ API مباشرة (تحضيراً لتطبيق الموبايل)

بمستخدم مدير الفرع، من أي عميل REST (Postman أو `curl` بجلسة/توكن صالح):

```bash
curl -X POST https://e-u40.hosetia.com/api/method/asset_mgmt_custom.api.branch_manager.get_dashboard_summary \
  -H "Authorization: token <api_key>:<api_secret>"
```

تأكد أن الأرقام المُرجَعة تخص فرعه فقط. كرِّر مع `list_my_assets`،
`get_asset_detail`، `create_maintenance_request` (بجسم JSON يحمل
`asset` و`problem_description`)، وتأكد أن كل استدعاء بـ `asset` من فرع
آخر يرجع خطأ صلاحية (`PermissionError`) وليس بيانات.

---

## قرارات صُممت عمداً بهذا الشكل

- **رفض طلب الصيانة نهائي بدون رجوع** — بناءً على تأكيدك الصريح، وليس
  افتراضاً.
- **التوزيع التلقائي على الفنيين بقي بمستوى الفئة فقط** — لم يُعدَّل رغم
  إتاحة توسيعه لاحقاً (فرع/جهاز)، بناءً على تأكيدك الصريح.
- **الدور "Branch Manager" يُضاف تلقائياً، لا يُزال تلقائياً أبداً** —
  إزالة صلاحية شخص من الوصول الكامل قرار حساس يجب أن يبقى يدوياً.
- **التذكيرات = ToDo قياسي، وليست DocType جديد** — Frappe يحل هذا
  المفهوم بالفعل؛ اختراع مفهوم مواز كان سيخالف مبدأ "لا تُنشئ ما هو
  موجود بالفعل" المُتَّبع في كل هذا التطبيق.
- **صورة العطل تُرفَع كملف منفصل بعد الإنشاء، وليست معاملاً في
  `create_maintenance_request`** — هذا هو النمط القياسي لأي عميل REST/
  موبايل يتعامل مع Frappe (الملف الثنائي لا يُرسَل داخل جسم JSON).

---

## حدود ما تم تنفيذه

- لا يوجد تطبيق موبايل فعلي هنا — فقط طبقة الـ API التي سيستهلكها.
- لا يوجد اختبار تكاملي فعلي نُفِّذ من هذه الجلسة (موضّح بالتفصيل أعلى
  الملف) — فقط تحقق سكوني دقيق. الاختبار الفعلي المطلوب موثَّق خطوة
  بخطوة في القسم السابق وينتظر تنفيذك على السيرفر الحقيقي.
- لم تُمس صلاحيات أي دور موجود من قبل (Asset Manager، Asset Technician،
  Asset Finance Manager، Asset User، Employee) — فقط أُضيف دور جديد
  ومسارات جديدة بجانبها.
- التوزيع الجغرافي أعلى من الفرع (Territory، من عمل سابق) لم يُدمَج بعد
  في منطق التقييد هنا — الوحدة الحالية للتقييد هي الفرع فقط، كما طُلب
  تحديداً.
