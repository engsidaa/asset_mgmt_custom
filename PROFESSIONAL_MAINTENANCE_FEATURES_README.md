# مزايا الصيانة الاحترافية — التوثيق الكامل

توثيق دقيق للتسعة بنود التي تمت الموافقة عليها بالكامل ("ابدأ بكل شيء
فوراً") — 4 تحسينات سهولة استخدام + 5 مزايا احترافية كبيرة. كل بند هنا
تم تنفيذه فعلياً ودُفع للفرع `claude/asl-aliye-amal-pmmeul`، وتم التحقق
منه سكونياً (syntax صحيح لكل ملف) — **لا يوجد اختبار تكاملي فعلي من هنا**
(لا يوجد site متصل في بيئة التنفيذ)، فخطوات الاختبار اليدوي أسفل كل بند
لازم تُنفَّذ بواسطتك بعد الترحيل.

---

## أ) سهولة الاستخدام

### 1. Kanban Board لأمر العمل
لوحة "متابعة أوامر العمل" (Workspace shortcut) — أعمدة بألوان مطابقة
للحالات الست (مفتوح/قيد التنفيذ/معلق/مكتمل/مرفوض/ملغي)، سحب وإفلات
بدل فتح كل نموذج. **اختبار:** افتح Workspace "الصيانة" ← اضغط "متابعة
أوامر العمل (Kanban)" ← اسحب بطاقة بين عمودين وتأكد أن الحالة تغيّرت
فعلياً على المستند.

### 2. نموذج طلب صيانة مبسَّط (Quick Entry)
`title` لم يعد إجبارياً (يُولَّد تلقائياً من اسم الأصل + وصف المشكلة)،
و`problem_description` أصبح هو الإجباري بدلاً منه. **اختبار:** من قائمة
Asset Work Order، اضغط "+ New" — يجب أن يظهر نموذج Quick Entry مختصر
(الأصل، نوع العمل، الأولوية، وصف المشكلة فقط) بدل النموذج الكامل.

### 3. QR Code لكل أصل
زر "Generate QR Code" على نموذج Asset — يُنشئ رمزاً يفتح مباشرة "أمر
عمل" جديد مُعبّأ بهذا الأصل عند مسحه. مطبوع تلقائياً على ملصق "Asset
Barcode Label" (الذي كان معطوباً فعلياً — كان يشير لدالة غير موجودة في
Frappe، تم اكتشافه وإصلاحه أثناء بناء هذه الميزة). **اختبار:** افتح أي
Asset ← اضغط "Generate QR Code" ← اطبع "Asset Barcode Label" وتأكد من
ظهور الرمز فعلياً (وليس صورة مكسورة) ← امسحه بكاميرا موبايل وتأكد أنه
يفتح نموذج أمر عمل جديد بالأصل مملوءاً مسبقاً.

### 4. تقويم أوامر العمل (Calendar View)
بُني على Asset Work Order (وليس Asset Maintenance Task المباشر — الأخير
جدول فرعي بلا Calendar View مستقل، وكل بند صيانة مستحق يُولِّد أمر عمل
تلقائياً أصلاً، فتقويم أوامر العمل يغطي الوقائي والتصحيحي معاً). فلترة
بالفرع ونوع العمل، ألوان حسب الحالة. **اختبار:** اضغط "تقويم أوامر
العمل" من Workspace ← تأكد من ظهور أوامر العمل في تواريخها الصحيحة
بألوان مطابقة لحالتها.

---

## ب) مزايا احترافية كبيرة

### 5. تنبيه فوري للأعطال الحرجة (واتساب/SMS)
DocType جديد **Asset Mgmt Settings** (Single) بحقول: تفعيل، رابط
Webhook، أرقام المستلمين. **لا يرسل واتساب/SMS مباشرة** (يحتاج بيانات
اعتماد بوابة فعلية غير متوفرة هنا) — يُسلِّم بيانات JSON لأي رابط تحدده
(Twilio مباشرة، أو أتمتة Zapier/Make/n8n تتولى الإرسال الفعلي). يعمل
تلقائياً عند إنشاء أمر عمل بأولوية "حرج"، وعند خرق SLA لأمر حرج.
**الإعداد المطلوب منك:** افتح **Asset Mgmt Settings** ← فعِّل التنبيهات
← أدخل رابط بوابتك الفعلية وأرقام المستلمين. بدون هذا الإعداد، الميزة
موجودة في الكود لكن لا تُرسِل شيئاً (لا خطأ، فقط لا تفعل شيئاً بصمت).

### 6. مراقبة سلسلة التبريد (Cold Chain)
`Asset Meter Reading` نوع عداد جديد "درجة الحرارة" + حقل `temperature_celsius`.
`Asset Category` حقلان جديدان: الحد الأدنى/الأقصى الآمن لدرجة الحرارة.
أي قراءة خارج النطاق (لفئة عندها حدود مُعرَّفة) تُنشئ أمر عمل طارئ
حرج تلقائياً — وهو بدوره يُطلق تنبيه الواتساب/SMS أعلاه تلقائياً (نفس
الآلية، بدون كود إضافي). **الإعداد المطلوب:** افتح فئة أصل تخص التبريد
(ثلاجات/فريزرات) ← أدخل الحد الأدنى/الأقصى الآمن. **اختبار:** أنشئ Asset
Meter Reading بنوع "درجة الحرارة" وقيمة خارج النطاق لأصل من هذه الفئة ←
تأكد من إنشاء أمر عمل طارئ تلقائياً بأولوية "حرج".

### 7. إعادة الطلب التلقائي لقطع الغيار
امتداد للمهمة المجدولة الموجودة أصلاً `check_spare_parts_low` — بالإضافة
للتنبيه، تُنشئ الآن **Material Request (مسودة)** تلقائياً لأي قطعة غيار
تحت حدها الأدنى، بهدف تخزين = ضعف الحد الأدنى، مع تفادي التكرار (تتحقق
من عدم وجود طلب شراء مُعلَّق بالفعل لنفس الصنف). **اختبار:** اضبط
`quantity` لقطعة غيار أقل من `minimum_qty` ← شغِّل
`bench execute asset_mgmt_custom.tasks.check_spare_parts_low` ← تأكد من
إنشاء Material Request جديد (مسودة) بنفس الصنف.

### 8. بوابة موردي الصيانة الخارجيين
دور جديد **Maintenance Vendor** — يرى ويعدّل **فقط** أوامر العمل
المُسندة له تحديداً (`assigned_technician` = مستخدمه)، عبر آلية Frappe
القياسية لهذا النوع من التقييد (`has_permission` +
`get_permission_query_conditions`، وليس User Permission — لأن الحقل
المُقيَّد به Link→User، وتقييده بـUser Permission كان سيُخفي كل
المستخدمين الآخرين من كل شاشات هذا المستخدم في النظام بالكامل). **اختبار:**
أنشئ مستخدماً بدور "Maintenance Vendor" ← أسنِد له أمر عمل عبر
`assigned_technician` ← سجّل دخول به ← تأكد أنه يرى هذا الأمر فقط (لا
غيره)، ويقدر يضغط "إتمام"/"رفض" عليه فقط.

### 9. مؤشر صحة الفرع (Branch Health Score)
تقرير جديد يجمع 4 مؤشرات موجودة بالفعل (نسبة تكلفة الصيانة، عدد خروقات
SLA، ساعات التوقف، أوامر العمل المفتوحة) في رقم واحد (0-100) لكل فرع،
مُرتَّب من الأسوأ للأفضل، بصيغة ترجيح موثَّقة بالكامل داخل الملف نفسه
(قابلة للتعديل لاحقاً حسب خبرتكم التشغيلية الفعلية — هذه بداية منطقية
شفّافة وليست معادلة نهائية). **اختبار:** افتح تقرير "مؤشر صحة الفرع" من
Workspace "الصيانة" ← تأكد من ظهور كل فرع له أصول، مُرتَّباً بمؤشر الصحة
تصاعدياً (الأسوأ أولاً).

---

## خطوات الترحيل

```bash
cd ~/frappe-bench/apps/asset_mgmt_custom
git pull origin claude/asl-aliye-amal-pmmeul
cd ~/frappe-bench
bench migrate
bench restart
```

ثم امسح الكاش والـ Hard Refresh زي المرة اللي فاتت لو الـ Workspace لسه
مش عارض الاختصارات الجديدة فوراً.

## حدود ما تم تنفيذه
- تنبيه الواتساب/SMS: بنية التكامل فقط (Webhook قابل للتوصيل بأي بوابة) —
  لا يوجد إرسال فعلي بدون تكوين رابط حقيقي من عندكم، لعدم توفر بيانات
  اعتماد بوابة فعلية هنا.
- مؤشر صحة الفرع: صيغة ترجيح مبدئية موثَّقة صراحة كنقطة بداية، وليست
  معايرة نهائية مبنية على بيانات تاريخية فعلية (لا تتوفر هنا).
- لا اختبار تكاملي فعلي من هذه الجلسة — فقط تحقق سكوني دقيق لكل ملف.
