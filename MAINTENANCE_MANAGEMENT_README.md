# توثيق تطوير إدارة صيانة الأصول — المرحلة الثانية (Phase 2)

هذا المستند يوثِّق بدقة كل ما تم فحصه وتنفيذه في مرحلة "تطوير خصائص إدارة
صيانة الأصول" داخل هذا التطبيق (`asset_mgmt_custom`) — بدون إنشاء تطبيق أو
موديول منفصل، وبدون أي مستند مواز لما هو موجود بالفعل. كل تعديل مذكور هنا
تم فعلاً في الكود (مش اقتراح)، وتم اختباره بالتحويل (syntax-check) قبل كل
Commit، ودُفع بالفعل لفرع `claude/asl-aliye-amal-pmmeul`.

**مهم:** لا شيء هنا يعمل تلقائياً على السيرفر الفعلي حتى تُنفِّذ خطوات
[الترحيل المطلوبة](#خطوات-الترحيل-المطلوبة-بعد-git-pull) بنفسك (`git pull`
ثم `bench migrate` ثم `bench restart`) — هذه الصلاحية غير متاحة من هنا.

---

## جدول المحتويات

1. [نتائج فحص التطبيق قبل التنفيذ](#نتائج-فحص-التطبيق-قبل-التنفيذ)
2. [الفجوات الحقيقية التي عولجت (10 بنود)](#الفجوات-الحقيقية-التي-عولجت-10-بنود)
3. [البند 1: ربط الفرع بمركز التكلفة والمنطقة الجغرافية](#البند-1-ربط-الفرع-بمركز-التكلفة-والمنطقة-الجغرافية)
4. [البند 2: القيد المحاسبي التلقائي عند إتمام أمر العمل](#البند-2-القيد-المحاسبي-التلقائي-عند-إتمام-أمر-العمل)
5. [البند 3: حركة مخزون حقيقية عند صرف قطعة الغيار](#البند-3-حركة-مخزون-حقيقية-عند-صرف-قطعة-الغيار)
6. [البند 4: توليد أمر عمل تلقائياً من بند صيانة مستحق](#البند-4-توليد-أمر-عمل-تلقائياً-من-بند-صيانة-مستحق)
7. [البند 5: سياسة SLA قابلة للتهيئة](#البند-5-سياسة-sla-قابلة-للتهيئة)
8. [البند 6: تخصص الفني والتوزيع التلقائي حسب الحمل](#البند-6-تخصص-الفني-والتوزيع-التلقائي-حسب-الحمل)
9. [البند 7: القيمة الدفترية وقرار الإصلاح مقابل الاستبدال في تقرير TCO](#البند-7-القيمة-الدفترية-وقرار-الإصلاح-مقابل-الاستبدال-في-تقرير-tco)
10. [البند 8: إثراء قاموس الأعطال](#البند-8-إثراء-قاموس-الأعطال)
11. [البند 9: قائمة قطع الغيار القياسية لكل فئة أصل](#البند-9-قائمة-قطع-الغيار-القياسية-لكل-فئة-أصل)
12. [خطوات الترحيل المطلوبة بعد git pull](#خطوات-الترحيل-المطلوبة-بعد-git-pull)
13. [بيانات لازم تُدخَل يدوياً (لا تُنشَأ تلقائياً)](#بيانات-لازم-تُدخَل-يدوياً-لا-تُنشَأ-تلقائياً)
14. [قائمة كل الـ Commits والملفات المتأثرة](#قائمة-كل-الـ-commits-والملفات-المتأثرة)
15. [حدود ما تم تنفيذه (Out of Scope)](#حدود-ما-تم-تنفيذه-out-of-scope)

---

## نتائج فحص التطبيق قبل التنفيذ

قبل كتابة أي كود، تم فحص التطبيق الحالي فعلياً (عبر قراءة JSON الخاص بكل
DocType، وبحث نصي `grep` في الكود، وليس افتراضاً) للإجابة على 5 أسئلة
إلزامية:

| # | السؤال | الإجابة الفعلية بعد الفحص |
|---|--------|---------------------------|
| 1 | ما الذي يمثل "الفرع"؟ | DocType أساسي في ERPNext اسمه **Branch** (وليس مخصصاً). له بالفعل 3 حقول مخصصة: `custom_branch_manager` (Link→User)، `custom_default_warehouse` (Link→Warehouse)، `custom_default_location` (Link→Location). **لا يوجد** حقل Cost Center عليه. |
| 2 | هل يوجد مستوى جغرافي أعلى من الفرع؟ | **لا يوجد إطلاقاً.** بحث `grep` عن "Territory" في كل كود التطبيق رجع صفر نتائج — لا استخدام لـ Territory، ولا شجرة Zone/Region مخصصة. الأصل مربوط مباشرة بالفرع بدون أي مستوى فوقه. |
| 3 | كيف يُمثَّل الأصل؟ | DocType **Asset** الأساسي في ERPNext بكل حقوله القياسية (asset_category، gross_purchase_amount، available_for_use_date، cost_center...)، بالإضافة إلى **24 حقلاً مخصصاً** موجودة بالفعل من عمل سابق على هذا التطبيق (منها `custom_branch`، `custom_total_maintenance_cost`، `custom_last_maintenance_date`، `custom_total_downtime_hours`، `custom_under_warranty`...). |
| 4 | ما العلاقة الحالية بين الفرع/الأصل ومركز التكلفة؟ | `Location.custom_cost_center` (Link→Cost Center) موجود ومُستخدَم فعلياً (يُقترَح تلقائياً عند نقل أصل عبر Asset Movement). **الفرع نفسه بلا Cost Center مباشر — فجوة حقيقية مؤكدة.** على مستوى الفئة، `Asset Category Account` عنده بالفعل 3 حقول محاسبية مخصصة من عمل سابق: `custom_capital_maintenance_wip_account`، `custom_maintenance_expense_account`، `custom_maintenance_accrued_liability_account`. |
| 5 | هل الإهلاك يعمل فعلياً؟ | **نعم، تم اختباره فعلياً** هذا الموسم (12 قسطاً شهرياً نظيفاً بقيمة 2000 لكل قسط، بعد إصلاح خطأ في محاذاة التاريخ)، مع حسابات إهلاك متراكم ومصروف إهلاك مربوطة على فئة الأصل. |

**اكتشاف إضافي مهم:** التطبيق يحتوي بالفعل على بنية صيانة كبيرة جداً (~60
DocType)، منها ما يتقاطع مباشرة مع قائمة المزايا المرجعية المطلوبة: `Asset
Work Order`، `Asset Maintenance Contract`، `Asset Maintenance Budget`،
`Asset Spare Part` + `Asset Spare Part Request`، `Asset Failure Analysis`،
`Asset Replacement Plan`، `Asset Meter Reading`، `Asset Calibration
Record`، `Asset Safety Inspection`، `Asset Risk Assessment`، `Asset
Physical Audit`، `Asset Vendor Contract`، `Asset Vendor Performance
Rating`، `Asset KPI Target`، `Asset CapEx Budget`. كذلك نظام الصيانة
الوقائية الأساسي في ERPNext نفسه (`Asset Maintenance` + `Asset Maintenance
Task` + `Asset Maintenance Log`) مُستخدَم جزئياً فعلاً (تنبيهات فقط، بدون
توليد أمر عمل).

بناءً على هذا الفحص، تقرر **عدم إنشاء أي DocType مواز** لأي مما سبق —
التنفيذ اقتصر على سد الفجوات الحقيقية فقط عبر حقول مخصصة، جدول فرعي واحد،
و DocType جديد واحد فقط لمفهوم لم يكن له أي تمثيل سابق إطلاقاً (سياسة SLA).

---

## الفجوات الحقيقية التي عولجت (10 بنود)

هذه هي الفجوات العشرة التي حُدِّدت بعد الفحص أعلاه، بالترتيب الذي نُفِّذت
به (البند 9 و10 من الخطة الأصلية دُمجا لأنهما مرتبطان مباشرة بنفس
الـ Commit):

1. لا يوجد ربط Cost Center↔Branch.
2. لا يوجد مستوى جغرافي أعلى من الفرع (Territory).
3. `Asset Work Order` لا يُنشئ أي قيد محاسبي عند الإتمام رغم امتلاكه حقول
   تكلفة كاملة.
4. `Asset Spare Part Request` لا يُنشئ أي حركة مخزون حقيقية عند "الصرف".
5. لا يوجد ربط تلقائي من بند صيانة مستحق (`Asset Maintenance Task`) إلى
   أمر عمل فعلي — فقط تنبيه.
6. مهلة SLA غير قابلة للتهيئة (ثوابت ثابتة 48 ساعة / 3 أيام مكتوبة مباشرة
   في الكود).
7. لا يوجد تمثيل لتخصص الفني أو حمل عمله لأغراض التوزيع التلقائي.
8. تقرير التكلفة الإجمالية للملكية (TCO) يستخدم سعر الشراء الأصلي فقط، ولا
   يوجد أي منطق "إصلاح مقابل استبدال" في كل الكود.
9. لا يوجد قاموس أعطال منظَّم (Fault Dictionary).
10. لا توجد قائمة قطع غيار قياسية (BOM) لكل فئة أصل.

---

## البند 1: ربط الفرع بمركز التكلفة والمنطقة الجغرافية

**الفجوة:** لا يوجد أي حقل على `Branch` يربطه بـ Cost Center أو بأي مستوى
جغرافي أعلى منه — فأي تقرير محاسبي أو تقرير مصنَّف حسب المنطقة كان
مستحيلاً على مستوى الفرع.

**ما تم فعلاً** (في `asset_mgmt_custom/fixtures/custom_field.json`):

- حقل جديد `Branch.custom_cost_center` — نوعه Link إلى **Cost Center**،
  يظهر بعد `custom_default_location` مباشرة.
- حقل جديد `Branch.custom_territory` — نوعه Link إلى **Territory** (تم
  إعادة استخدام DocType "Territory" الأساسي والمُدمَج فعلاً في ERPNext
  Core — **لم يُنشأ أي DocType جديد**)، يظهر بعد `custom_cost_center`.

كلا الحقلين اختياريان (`reqd: 0`) — لن يمنعا حفظ أي فرع قائم.

**كيف تختبره يدوياً:**

1. افتح **الفروع (Branch List)** ← اختر أي فرع موجود ← Edit.
2. تأكد من ظهور حقلين جديدين: "مركز التكلفة (Cost Center)" و"المنطقة
   الجغرافية (Territory)" أسفل "الموقع الافتراضي للفرع".
3. اختر مركز تكلفة حقيقياً من شجرة الحسابات، واحفظ. لا حاجة لملء
   Territory إذا لم تكن تستخدمه بعد.

---

## البند 2: القيد المحاسبي التلقائي عند إتمام أمر العمل

**الفجوة:** `Asset Work Order` كان يملك حقول تكلفة كاملة
(`estimated_cost`، `actual_cost`، `spare_parts_cost`، `labor_cost`،
`cost_center`) لكنه **لم يُنتج أي قيد محاسبي إطلاقاً**. كذلك، حالة
(`status`) الحقل لم تكن `allow_on_submit`، فكان من المستحيل فعلياً تغيير
حالة أمر عمل مُعتمَد إلى "مكتمل" من واجهة النظام (كل الحقول تُقفَل بعد
الاعتماد افتراضياً في Frappe إلا ما صُرِّح له صراحة).

**ما تم فعلاً:**

- في `asset_work_order.json`: أُضيف `allow_on_submit: 1` على الحقول:
  `status`، `start_date`، `completion_date`، `actual_hours`،
  `completion_notes`، `actual_cost`، `spare_parts_cost`، `labor_cost` —
  حتى يمكن فعلاً إكمال أمر العمل بعد اعتماده.
- حقل جديد `journal_entry` (Link→Journal Entry، للقراءة فقط) يُسجَّل فيه
  القيد الناتج تلقائياً.
- في `asset_work_order.py`: دالة جديدة `on_update_after_submit` تُنفَّذ
  في كل مرة يُحفَظ فيها المستند بعد اعتماده. إذا كانت الحالة "مكتمل"،
  تستدعي `_post_maintenance_cost_gl_entry()` التي:
  1. **تتحقق أولاً** أن `journal_entry` غير موجود مسبقاً (Idempotent — لن
     يُنشأ قيد مكرر أبداً حتى لو حُفظ المستند عدة مرات).
  2. تحسب `total_cost = actual_cost` (أو `labor_cost + spare_parts_cost`
     إذا كان `actual_cost` فارغاً).
  3. تقرأ من `Asset Category Account` الخاص بفئة أصل هذا الأمر:
     `custom_maintenance_expense_account` (مدين) و
     `custom_maintenance_accrued_liability_account` (دائن) — نفس الحسابين
     المُستخدَمين بالفعل في تدفق `Asset Repair` من عمل سابق، فلا يوجد أي
     ازدواج في المنطق المحاسبي.
  4. تحدد مركز التكلفة بالترتيب: `cost_center` المُدخَل يدوياً على أمر
     العمل ← فإذا فارغ، `custom_cost_center` الخاص بفرع الأصل (البند 1
     أعلاه) ← فإذا فارغ أيضاً، `cost_center` الخاص بالأصل نفسه.
  5. تُنشئ Journal Entry وتعتمدها (submit)، ثم تسجل اسمها على
     `self.journal_entry`.
  - عند الإلغاء (`on_cancel`)، القيد المرتبط يُلغى تلقائياً (لو موجود
    ومُعتمَد).
- زر جديد في واجهة أمر العمل (`asset_work_order.js`): **"إتمام أمر
  العمل"** يظهر فقط على أمر عمل مُعتمَد لم يُغلَق بعد، يغيّر الحالة إلى
  "مكتمل" مباشرة (بدلاً من تعديل الحقل يدوياً)، وزر **"عرض قيد اليومية"**
  يظهر بمجرد وجود قيد مرتبط.
- **تحديث جانبي مهم:** الدالة المشتركة `_update_asset_maintenance_summary`
  في `overrides/asset_repair.py` (التي تُحدِّث
  `Asset.custom_total_maintenance_cost` و `custom_last_maintenance_date`)
  كانت تجمع فقط من `Asset Repair`. أصبحت الآن تجمع (`UNION ALL`) من
  **كلا المصدرين معاً**: `Asset Repair` و`Asset Work Order` — بحيث
  الإجمالي المعروض على الأصل نفسه يعكس التكلفة الحقيقية عبر عمره الكامل،
  بغض النظر عن أي مستند صدرت منه.

**متطلب إعداد مسبق إلزامي (بدونه سيفشل القيد برسالة واضحة):** لازم يكون
لكل فئة أصل (Asset Category) حساب مصروف صيانة وحساب التزامات مستحقة
مُعرَّفين في `Asset Category Account` (نفس الحسابين المُستخدَمين في
Asset Repair — لو مضبوطين له بالفعل، فهما جاهزان هنا أيضاً بدون أي تعديل
إضافي).

**كيف تختبره يدوياً:**

1. افتح **Asset Work Order** موجود ومُعتمَد (docstatus = 1)، وله
   `actual_cost` أو `labor_cost`/`spare_parts_cost` أكبر من صفر.
2. اضغط زر **"إتمام أمر العمل"** في أعلى الصفحة ← أكِّد.
3. بعد إعادة التحميل، تأكد أن الحالة أصبحت "مكتمل"، وأن حقل "قيد اليومية"
   امتلأ باسم Journal Entry، وأن زر "عرض قيد اليومية" ظهر.
4. افتح القيد نفسه وتأكد أن طرفيه صحيحان (مدين: مصروف الصيانة، دائن:
   التزامات صيانة مستحقة) وبنفس مركز التكلفة المتوقع.
5. افتح الأصل المرتبط وتأكد أن `custom_total_maintenance_cost` تحدَّث
   ليشمل هذه التكلفة.
6. جرّب الضغط على "إتمام أمر العمل" مرة أخرى (أو أعِد حفظ المستند) — تأكد
   أنه **لا يُنشأ قيد ثانٍ** (فحص الـ Idempotency).

---

## البند 3: حركة مخزون حقيقية عند صرف قطعة الغيار

**الفجوة:** `Asset Spare Part Request.on_submit()` كان فقط ينفِّذ
`self.db_set("status", "Approved")` — أي "صرف" لاحق كان مجرد تغيير حالة،
بدون أي أثر فعلي على المخزون في ERPNext، وبدون نقص فعلي في
`Asset Spare Part.quantity`.

**ما تم فعلاً:**

- حقل جديد `stock_entry` (Link→Stock Entry، للقراءة فقط).
- `allow_on_submit: 1` على `status` و`quantity_issued` (لنفس سبب البند 2
  — بدونه لا يمكن تغيير الحالة إلى "Issued" بعد الاعتماد).
- دالة مُصرَّح بها (`@frappe.whitelist()`) جديدة `issue_spare_part()`:
  1. تتحقق أن الحالة "Approved" (وليست أي حالة أخرى).
  2. تتحقق أن `stock_entry` غير موجود مسبقاً (Idempotent).
  3. تتحقق أن قطعة الغيار (`Asset Spare Part`) المرتبطة معرَّف لها فعلاً
     `item_code` و`warehouse` — وإلا رسالة خطأ واضحة تطلب ربطها أولاً.
  4. تتحقق أن الكمية المطلوبة لا تتجاوز الكمية المتاحة فعلياً
     (`Asset Spare Part.quantity`).
  5. تُنشئ **Stock Entry حقيقي من نوع "Material Issue"** من مستودع قطعة
     الغيار، بمركز تكلفة من فرع الأصل (نفس منطق البند 2)، وحساب مصروف من
     `custom_maintenance_expense_account` الخاص بفئة الأصل (لتوحيد وجهة
     المصروف مع أوامر العمل).
  6. تُنقِص `Asset Spare Part.quantity` بمقدار الكمية المصروفة فعلياً.
  7. تسجل اسم الحركة على `self.stock_entry`، وتغيّر الحالة إلى "Issued".
  - عند الإلغاء (`on_cancel`)، الحركة تُلغى والكمية تُعاد فعلياً إلى
    `Asset Spare Part.quantity` (باستخدام `quantity_issued` المُسجَّلة على
    الطلب نفسه، وليس بمطابقة الصنف عكسياً — تفادياً لتضارب لو عدة قطع غيار
    تتشارك نفس الـ Item).
- زر جديد في الواجهة (`asset_spare_part_request.js`): **"صرف القطعة
  (Stock Entry)"** يظهر فقط على طلب مُعتمَد بحالة "Approved" وبدون حركة
  مخزون سابقة، وزر **"عرض حركة المخزون"** بمجرد وجودها.

**متطلب إعداد مسبق:** كل `Asset Spare Part` تريد صرفها فعلياً لازم يكون
لها `item_code` (صنف حقيقي في ERPNext) و`warehouse` (مستودع حقيقي) مربوطين
— القطع التي بلا هذا الربط ستبقى قابلة للطلب والاعتماد كما كانت، لكن زر
"الصرف" سيرفض التنفيذ برسالة واضحة.

**كيف تختبره يدوياً:**

1. تأكد أن قطعة غيار (`Asset Spare Part`) لها `item_code` و`warehouse`
   حقيقيان، وأن المستودع به رصيد كافٍ من هذا الصنف.
2. أنشئ **Asset Spare Part Request** جديداً بهذه القطعة، اعتمده (Submit) —
   الحالة تصبح "Approved" تلقائياً.
3. اضغط **"صرف القطعة (Stock Entry)"** ← أكِّد.
4. تأكد أن الحالة أصبحت "Issued"، وأن حقل "حركة المخزون" امتلأ.
5. افتح الـ Stock Entry نفسه وتأكد أنه من نوع Material Issue وبنفس الكمية
   والمستودع الصحيحين.
6. افتح `Asset Spare Part` وتأكد أن `quantity` نقصت فعلياً بنفس المقدار.

---

## البند 4: توليد أمر عمل تلقائياً من بند صيانة مستحق

**الفجوة:** المهمة المجدولة `send_maintenance_due_alerts()` في
`tasks.py` كانت تربط `Asset Maintenance Task` بـ `Asset Maintenance` وترسل
تنبيهاً فقط عند استحقاق أي بند صيانة — بدون أي مستند فعلي يُنشأ. حقل
`Asset Work Order.maintenance_schedule` (Link→Asset Maintenance) كان
موجوداً بالفعل من قبل لكن لا شيء يملؤه.

**ما تم فعلاً:**

- حقل جديد `Asset Work Order.source_maintenance_task` — نوعه **Data** (لا
  Link، لأن `Asset Maintenance Task` هو جدول فرعي `istable: 1` وليس
  مستنداً مستقلاً، فلا يصلح كخيارات لحقل Link) يخزّن المُعرِّف الداخلي
  (name) لصف بند الصيانة الذي وَلَّد هذا الأمر — يُستخدَم فقط لمنع التكرار.
- دالة جديدة `_auto_create_work_order_from_task()` في `tasks.py`:
  - تتحقق أولاً (`frappe.db.exists`) أنه لا يوجد بالفعل `Asset Work Order`
    بنفس `source_maintenance_task` وبحالة docstatus < 2 (Idempotent).
  - تُنشئ **مسودة** (بدون اعتماد تلقائي) `Asset Work Order` جديدة:
    `asset` و`branch` من الأصل نفسه، `work_type = "صيانة وقائية"`،
    `priority = "عادي"` (افتراضي)، `maintenance_schedule` من نفس السجل،
    `assigned_technician` من `assign_to` الخاص ببند الصيانة إن وُجد،
    ووصف تلقائي في `problem_description` يذكر تاريخ الاستحقاق.
- `send_maintenance_due_alerts()` تستدعي هذه الدالة **فقط** للبنود
  المستحقة فعلياً (`days_left <= 0`، أي مستحقة اليوم أو متأخرة) — البنود
  التي لا تزال "قريبة الاستحقاق" (خلال 7 أيام القادمة) تبقى تحصل على
  التنبيه فقط كما كان، بدون توليد أمر عمل مبكراً.
- أي خطأ أثناء الإنشاء التلقائي (مثلاً بيانات ناقصة) يُسجَّل في
  `frappe.log_error` بدلاً من إيقاف باقي المهمة المجدولة.

**كيف تختبره يدوياً (بدون انتظار الجدولة اليومية):**

1. افتح **Asset Maintenance** لأصل موجود، وفي جدول `asset_maintenance_tasks`
   عدِّل `next_due_date` ليكون تاريخ اليوم أو تاريخ ماضٍ، و`maintenance_status`
   لأي شيء غير "Completed"، ثم احفظ.
2. من واجهة **Bench Console** (أو زر "Run" على مهمة مجدولة إن كان متاحاً
   للمستخدم)، شغِّل:
   `bench execute asset_mgmt_custom.tasks.send_maintenance_due_alerts`
3. افتح قائمة **Asset Work Order** وابحث عن أمر جديد بحالة "مفتوح"، فيه
   `work_type = "صيانة وقائية"` ومربوط بنفس الأصل، وحقل
   `maintenance_schedule` يشير لنفس سجل Asset Maintenance.
4. شغِّل نفس الأمر مرة ثانية وتأكد أنه **لم يُنشأ أمر عمل مكرر** لنفس بند
   الصيانة.

---

## البند 5: سياسة SLA قابلة للتهيئة

**الفجوة:** `tasks.py` كان به ثابتان مكتوبان مباشرة في الكود:
`check_requisition_sla(sla_hours=48)` و`check_overdue_work_orders()` (3
أيام ثابتة) — لا علاقة بينهما وبين أولوية أمر العمل، ولا أي آلية تصعيد
حقيقية.

**ما تم فعلاً — DocType جديد بالكامل (المبرَّر الوحيد لإضافة DocType جديد
في هذه المرحلة، لأن هذا المفهوم لم يكن له أي تمثيل سابق):**

**`Asset Maintenance SLA Policy`** (غير قابل للاعتماد، `autoname:
field:priority` — أي أن اسم كل سجل هو نفسه قيمة الأولوية، فلا يمكن وجود
أكثر من سياسة لنفس الأولوية):

| الحقل | النوع | الوصف |
|-------|------|-------|
| `priority` | Select | يجب أن يطابق خيارات حقل الأولوية في Asset Work Order تماماً: عادي / متوسط / عاجل / حرج |
| `response_hours` | Float | مهلة الاستجابة بالساعات من إنشاء أمر العمل |
| `resolution_hours` | Float | مهلة الحل (الإتمام) بالساعات من إنشاء أمر العمل |
| `escalation_level` | Select | مستوى ١/٢/٣ (وصفي فقط) |
| `escalate_to` | Link→User | مستخدم إضافي يُبلَّغ عند خرق مهلة الحل لهذه الأولوية تحديداً (اختياري) |

**التكامل مع Asset Work Order:**

- حقول جديدة: `sla_policy` (Link، للقراءة فقط)، `response_due_by`
  (Datetime، للقراءة فقط)، `resolution_due_by` (Datetime، للقراءة فقط)،
  `sla_breached` (Check، `allow_on_submit`).
- دالة `_apply_sla_policy()` تُنفَّذ في `before_insert` (أي **مرة واحدة
  فقط** عند إنشاء أمر العمل، وليست قابلة لإعادة الحساب لاحقاً — حتى لو
  عُدِّلت الأولوية لاحقاً، تبقى المواعيد كما حُسبت عند الإنشاء): تبحث عن
  سياسة SLA مطابقة لأولوية الأمر، وإن وُجدت تحسب
  `response_due_by = الآن + response_hours` و
  `resolution_due_by = الآن + resolution_hours`.
- في `tasks.py`، دالة `check_overdue_work_orders()` أُعيدت كتابتها بالكامل:
  تبحث الآن عن أوامر عمل تجاوزت `resolution_due_by` الفعلي (وليس ثابتاً
  عاماً)، وتُعلِّم `sla_breached = 1`، وتُبلِّغ مدراء الأصول **و**
  `escalate_to` الخاص بالسياسة إن وُجد. أوامر العمل التي **لا** توجد لها
  سياسة SLA مطابقة (لم تُنشأ بعد أي سياسة لأولويتها) تستمر على القاعدة
  القديمة (3 أيام منذ الطلب) كاحتياط، حتى لا تفلت من أي تنبيه.
- `check_requisition_sla()` (الخاصة بموافقات طلب الأصل، وليس الصيانة) **لم
  تُمَس** — مفهوم مختلف تماماً (سلسلة اعتماد شراء، لا علاقة له بأولوية
  الصيانة)، فتحويلها لنفس آلية SLA كان سيقحم شيئاً لا علاقة له بالمهمة.

**كيف تختبره يدوياً:**

1. افتح **Asset Maintenance SLA Policy** ← جديد. أنشئ 4 سجلات (واحد لكل
   قيمة أولوية: عادي، متوسط، عاجل، حرج) بساعات استجابة/حل منطقية (مثلاً:
   حرج = 2/8، عاجل = 4/24، متوسط = 8/48، عادي = 24/96).
2. أنشئ **Asset Work Order** جديداً بأولوية "حرج" واعتمده.
3. تأكد فوراً أن `sla_policy` و`response_due_by` و`resolution_due_by`
   امتلأت تلقائياً بالقيم الصحيحة (حاضر + الساعات المحددة في السياسة).
4. لاختبار الخرق: عدِّل مؤقتاً `resolution_due_by` على أمر العمل (مباشرة
   من قاعدة البيانات أو بتصغير الساعات في السياسة قبل إنشاء أمر تجريبي)
   ليكون في الماضي، ثم شغِّل:
   `bench execute asset_mgmt_custom.tasks.check_overdue_work_orders`
   وتأكد أن `sla_breached` أصبح 1 وأن إشعاراً وصل.

---

## البند 6: تخصص الفني والتوزيع التلقائي حسب الحمل

**الفجوة:** لا يوجد أي تمثيل لتخصص فني معيّن (Asset Category الذي يجيد
التعامل معه) ولا لحمل عمله الحالي — أي تكليف كان يدوياً بالكامل بدون أي
مساعدة من النظام.

**ما تم فعلاً — بدون أي DocType جديد، بإعادة استخدام `Maintenance Team
Member` (الجدول الفرعي الأساسي في ERPNext ضمن `Asset Maintenance Team`):**

- حقلان مخصصان جديدان على `Maintenance Team Member`:
  - `custom_skill_category` (Link→Asset Category) — فارغ يعني "فني عام"
    يصلح لأي فئة.
  - `custom_max_concurrent_orders` (Int، افتراضي 0 = بلا حد) — أقصى عدد
    أوامر عمل مفتوحة مسموح تكليف هذا الفني بها في نفس الوقت.
- دالة `_auto_dispatch_technician()` في `asset_work_order.py`، تُنفَّذ في
  `before_insert` **فقط إذا** حقل `assigned_technician` تُرك فارغاً (أي لا
  تتجاوز أبداً تكليفاً يدوياً صريحاً):
  1. تجلب فئة أصل وشركة الأصل المرتبط بأمر العمل.
  2. تبحث عن كل أعضاء فرق الصيانة (`Maintenance Team Member` عبر
     `Asset Maintenance Team`) في نفس الشركة، الذين تخصصهم (
     `custom_skill_category`) إما فارغ أو مطابق تماماً لفئة الأصل.
  3. لكل مرشح، تحسب حمله الحالي = عدد `Asset Work Order` المفتوحة
     (مفتوح/قيد التنفيذ) المُسندة له فعلياً، عبر `frappe.db.count`.
  4. تستبعد أي مرشح وصل لحد `custom_max_concurrent_orders` الخاص به.
  5. تختار الأقل حملاً من الباقين وتُسندها لـ `assigned_technician`.
  - إن لم يوجد أي مرشح مؤهل، لا تفعل شيئاً (يبقى التكليف اليدوي كما كان).

**متطلب إعداد مسبق:** لازم يكون لديك `Asset Maintenance Team` واحد على
الأقل (بنفس شركة أصولك) مع أعضاء (`Maintenance Team Member`) حقيقيين في
جدوله الفرعي، وإلا فلن يوجد أي "مرشح" على الإطلاق وسيبقى التكليف يدوياً
بصمت (بدون أي رسالة خطأ — هذا سلوك مقصود، وليس عطلاً).

**كيف تختبره يدوياً:**

1. افتح **Asset Maintenance Team** موجود (أو أنشئ واحداً) وأضف فنيّين
   (`team_member`) في الجدول الفرعي.
2. على أحد الأعضاء، اضبط `custom_skill_category` = فئة أصل معينة، وعلى
   الآخر اتركه فارغاً (عام)، واضبط `custom_max_concurrent_orders` = 1
   للأول لاختبار الحد الأقصى.
3. أنشئ **Asset Work Order** جديداً لأصل من نفس الفئة، بدون ملء
   `assigned_technician` يدوياً، واحفظ (Save كافٍ — الأمر ينفَّذ في
   `before_insert` قبل الاعتماد حتى).
4. تأكد أن `assigned_technician` امتلأ تلقائياً بأقل الفنيين حملاً.
5. أنشئ أمر عمل ثانٍ لنفس الفني (يدوياً) لتصل لحده الأقصى (1)، ثم أنشئ
   أمر عمل ثالث بدون تكليف يدوي — تأكد أن النظام تجاوز الفني الأول
   (وصل لحده) واختار غيره.

---

## البند 7: القيمة الدفترية وقرار الإصلاح مقابل الاستبدال في تقرير TCO

**الفجوة:** تقرير `asset_total_cost_of_ownership.py` كان يستخدم
`gross_purchase_amount` (سعر الشراء الأصلي) فقط في كل حساباته — لا إشارة
واحدة لـ `value_after_depreciation` في كل الملف. لا يوجد أي منطق "إصلاح
مقابل استبدال" في كل كود التطبيق (تم التأكد بالبحث النصي عن "repair.*replace"
و"economical" و"Beyond Economical" — لا نتائج خارج نص Select ثابت).

**ما تم فعلاً:**

- حقل جديد `Asset Category.custom_repair_replace_threshold_pct` (Percent،
  افتراضي 60%): النسبة التي إذا تجاوزتها تكلفة الصيانة التراكمية من
  القيمة الدفترية الحالية، يُوصى بالاستبدال بدل الإصلاح. **قابلة للتخصيص
  لكل فئة أصل على حدة.**
- عمود جديد في التقرير: **`book_value`** = `Asset.value_after_depreciation`
  (القيمة الفعلية بعد الإهلاك المتراكم؛ إن لم يكن الإهلاك مفعَّلاً بعد على
  الأصل، تعود تلقائياً لسعر الشراء الأصلي بدل قيمة فارغة أو صفر مضلِّلة).
- عمودان جديدان: **`repair_to_value_pct`** (النسبة المئوية لتكلفة الصيانة
  التراكمية من القيمة الدفترية) و**`recommendation`** ("Repair" أو
  "Consider Replacement")، محسوبان لكل أصل مقابل عتبة فئته الخاصة. أصل
  بقيمة دفترية صفر أو سالبة لكن له تكلفة صيانة فعلية يُصنَّف دائماً
  "Consider Replacement" (منطقياً: أي إنفاق على أصل مُهلَك بالكامل غير
  مبرَّر اقتصادياً).
- **ملاحظة مهمة:** صيغة إجمالي التكلفة (`tco`) نفسها **لم تُغيَّر** — لا
  تزال تجمع سعر الشراء الأصلي + كل التكاليف اللاحقة، لأن هذا هو التعريف
  المحاسبي الصحيح لـ "إجمالي تكلفة الملكية عبر العمر" (وليس شيئاً يُستبدَل
  بالقيمة الدفترية). القيمة الدفترية أُضيفت كعمود مستقل ومنطق قرار مستقل،
  وليست بديلاً لحساب TCO ذاته.

**كيف تختبره يدوياً:**

1. افتح **Asset Category** لفئة تستخدمها فعلياً، واضبط
   `custom_repair_replace_threshold_pct` (أو اتركها لتأخذ 60% الافتراضية).
2. افتح تقرير **Asset Total Cost of Ownership** (من قائمة التقارير).
3. تأكد من ظهور الأعمدة الجديدة: Current Book Value، Maintenance / Book
   Value %، Repair vs Replace.
4. اختر أصلاً قديماً نسبياً (إهلاك مفعَّل ومترحِّل) وله تاريخ صيانة/إصلاح
   متكرر، وتأكد أن النسبة والتوصية منطقيتان (كلما قلّت القيمة الدفترية
   وزادت تكلفة الصيانة، تزداد احتمالية "Consider Replacement").

---

## البند 8: إثراء قاموس الأعطال

**قرار حَكَمي (Judgment Call) صريح:** الخطة الأصلية تركت هذا البند مفتوحاً
بين خيارين: (أ) توسيع `failure_mode` الحالي + حقل `resolution_action`
جديد، أو (ب) DocType مستقل مخصص لقاموس أعطال. بعد فحص `Asset Failure
Analysis` الموجود فعلاً، تبيَّن أنه **يحتوي بالفعل** على `root_cause`،
`corrective_action`، `preventive_action`، و`lessons_learned` لكل حادثة —
أي أن إضافة حقل "resolution_action" منفصل كان سيكرر `corrective_action`
حرفياً. تم اختيار **الخيار الأخف** بدون DocType جديد:

- حقل جديد `symptom` (Small Text) في بداية قسم "تفاصيل العطل"، قبل
  `failure_mode` — يوثِّق **العرض الملاحَظ فعلياً** (صوت، اهتزاز، تسريب،
  توقف مفاجئ...) **قبل** أي تحليل لسببه، فيكتمل تسلسل "عرض ← سبب ← علاج"
  الذي كان ناقصاً خطوته الأولى فقط.
- توسيع خيارات `failure_mode` (Select) بثلاث فئات كانت ناقصة فعلياً من
  القائمة السابقة (9 خيارات): **تسريب**، **انسداد**، **عطل برمجي/تحكم**.
- دالة مُصرَّح بها جديدة `get_suggested_remedies(failure_mode, exclude)`
  في `asset_failure_analysis.py`: تبحث في كل سجلات `Asset Failure
  Analysis` السابقة (عبر كل الأصول) بنفس `failure_mode`، وتُرجع أكثر 10
  قيم `corrective_action` تكراراً (مرتبة تنازلياً حسب عدد مرات الاستخدام،
  مع تاريخ آخر استخدام) — أي **قاعدة معرفة حقيقية مبنية على البيانات
  الفعلية الموجودة**، بدون الحاجة لملء قاموس منفصل يدوياً من الصفر.
- زر جديد في الواجهة (`asset_failure_analysis.js`): **"اقتراحات حلول
  سابقة"** يظهر بمجرد اختيار `failure_mode`، يعرض النتائج في نافذة منبثقة.

**كيف تختبره يدوياً:**

1. افتح **Asset Failure Analysis** موجود (أو أنشئ 2-3 سجلات تجريبية بنفس
   `failure_mode` ولكن بقيم `corrective_action` مختلفة، بعضها مكرر).
2. افتح سجلاً بنفس `failure_mode` (سجل جديد أو موجود)، واضغط زر
   **"اقتراحات حلول سابقة"**.
3. تأكد من ظهور القيم السابقة مرتبة حسب التكرار، مع عدد مرات الاستخدام
   وتاريخ آخر استخدام، وأن السجل الحالي نفسه (لو مفتوحاً على سجل قائم)
   مُستبعَد من نتائجه.
4. تأكد أن الخيارات الثلاثة الجديدة (تسريب/انسداد/عطل برمجي) ظاهرة فعلاً
   في قائمة `failure_mode` عند إنشاء سجل جديد.

---

## البند 9: قائمة قطع الغيار القياسية لكل فئة أصل

**الفجوة:** لا توجد أي قائمة مرجعية لقطع الغيار المتوقع استهلاكها لأصل من
فئة معينة (BOM) — كل طلب قطعة غيار يبدأ من الصفر بدون أي مرجع سريع.

**ما تم فعلاً — DocType جديد واحد (جدول فرعي فقط، لا مستند مستقل):**

**`Asset Category Spare Part`** (`istable: 1`، غير قابل للاعتماد بطبيعته
كجدول فرعي):

| الحقل | النوع | الوصف |
|-------|------|-------|
| `spare_part` | Link→Asset Spare Part | القطعة نفسها |
| `spare_part_name` | Data (fetch، للقراءة فقط) | اسم القطعة، يُجلَب تلقائياً |
| `standard_qty` | Float (افتراضي 1) | الكمية المعتادة لكل صيانة/إصلاح |
| `notes` | Small Text | ملاحظات حرة |

يُضاف إلى `Asset Category` عبر حقل مخصص جديد من نوع **Table**:
`custom_standard_spare_parts`، داخل قسم قابل للطي (Collapsible) اسمه "قطع
الغيار القياسية (BOM)"، يظهر بعد حقل عتبة الاستبدال (البند 7).

**كيف تختبره يدوياً:**

1. افتح **Asset Category** لفئة تستخدمها فعلياً.
2. افتح القسم القابل للطي "قطع الغيار القياسية (BOM)" في أسفل الصفحة.
3. أضف صفاً أو أكثر: اختر `spare_part` من قائمة `Asset Spare Part`
   الموجودة، وتأكد أن `spare_part_name` يمتلئ تلقائياً، واضبط
   `standard_qty`.
4. احفظ، وأعِد فتح الفئة للتأكد أن الجدول محفوظ فعلاً.

---

## خطوات الترحيل المطلوبة بعد git pull

كل ما سبق موجود فقط في الكود المدفوع لفرع `claude/asl-aliye-amal-pmmeul`.
لتفعيله على السيرفر الفعلي (`e-u40.hosetia.com`):

```bash
cd ~/frappe-bench/apps/asset_mgmt_custom
git pull origin claude/asl-aliye-amal-pmmeul
cd ~/frappe-bench
bench migrate      # إلزامي — يوجد DocTypeان جديدان (Asset Maintenance SLA Policy،
                    # Asset Category Spare Part) وحقول جديدة كثيرة على DocTypes قائمة
bench restart
```

بعد نجاح `bench migrate` بدون أخطاء، راجع تحديداً القسم التالي — لأن لا
شيء فيه يحدث تلقائياً.

---

## بيانات لازم تُدخَل يدوياً (لا تُنشَأ تلقائياً)

هذه الخطوات **لن** تُنفَّذ من هنا (لا صلاحية وصول مباشر لقاعدة بيانات
الإنتاج) — لازم تُنفَّذها بنفسك من واجهة النظام بعد الترحيل:

1. **لكل فرع حقيقي تستخدمه:** افتح Branch ← اضبط `custom_cost_center`
   (لازم يكون Cost Center حقيقي موجود بالفعل في شجرة حسابات "كويت الغذاء
   (SAR)"). بدون هذا، القيود المحاسبية للبند 2 و3 ستقع رجوعاً على مركز
   تكلفة الأصل نفسه فقط (fallback)، وليس مركز تكلفة الفرع.
2. **لكل فئة أصل (Asset Category) تستخدمها للصيانة:** تأكد من وجود
   `custom_maintenance_expense_account` و
   `custom_maintenance_accrued_liability_account` في `Asset Category
   Account` الخاص بها وبشركتك — **بدون هذين الحسابين، أي محاولة لإتمام
   Asset Work Order بتكلفة أو صرف Asset Spare Part Request ستفشل برسالة
   خطأ واضحة تطلب ضبطهما أولاً** (هذا سلوك مقصود لمنع قيد بلا حساب صحيح،
   وليس عطلاً).
3. **أنشئ 4 سجلات `Asset Maintenance SLA Policy`** (واحد لكل قيمة أولوية:
   عادي، متوسط، عاجل، حرج) — بدونها، أوامر العمل الجديدة لن تحصل على
   `response_due_by`/`resolution_due_by`، وستستمر `check_overdue_work_orders`
   على القاعدة الاحتياطية القديمة (3 أيام) بدل SLA حقيقي. **لم يُشحَن أي
   سجل افتراضي تلقائياً عمداً** — نفس نهج التطبيق في عدم افتراض بيانات
   محاسبية أو تشغيلية خاصة بشركتك.
4. **(اختياري لكن مُوصى به) لكل `Asset Maintenance Team`:** اضبط
   `custom_skill_category` و`custom_max_concurrent_orders` على أعضاء
   الفريق (`Maintenance Team Member`) — بدون هذا، التوزيع التلقائي (البند
   6) لن يجد أي مرشح، وسيبقى التكليف يدوياً بصمت.
5. **(اختياري) لكل فئة أصل:** اضبط `custom_repair_replace_threshold_pct`
   إن كانت 60% الافتراضية لا تناسب طبيعة أصولها، واملأ
   `custom_standard_spare_parts` (البند 9) لتسهيل طلبات القطع لاحقاً.
6. **(اختياري) لكل فرع:** اربط `custom_territory` إن قررت البدء فعلياً في
   استخدام مستوى جغرافي أعلى من الفرع في تقاريرك — الحقل موجود وجاهز لكن
   لا شيء يعتمد عليه إلزامياً بعد.

---

## قائمة كل الـ Commits والملفات المتأثرة

كل commit التالي على فرع `claude/asl-aliye-amal-pmmeul`، بالترتيب:

| Commit | الوصف | الملفات المتأثرة |
|--------|-------|-------------------|
| `487375c` | ربط Branch بـ Cost Center/Territory + قيد GL عند إتمام أمر العمل | `custom_field.json`، `asset_work_order.{json,py,js}`، `overrides/asset_repair.py` |
| `00fa3a3` | Stock Entry حقيقي عند صرف قطعة الغيار | `asset_spare_part_request.{json,py,js}` |
| `2790f19` | توليد أمر عمل تلقائياً من بند صيانة مستحق | `asset_work_order.json`، `tasks.py` |
| `fd84f72` | DocType سياسة SLA + ربطها بأمر العمل | `asset_maintenance_sla_policy/*` (جديد)، `asset_work_order.{json,py}`، `tasks.py` |
| `d1bdd63` | تخصص الفني والتوزيع التلقائي | `asset_work_order.py`، `custom_field.json`، `hooks.py` |
| `88c39c7` | القيمة الدفترية وقرار الإصلاح/الاستبدال في TCO | `asset_total_cost_of_ownership.py`، `custom_field.json` |
| `ac3d4fb` | إثراء Asset Failure Analysis | `asset_failure_analysis/*` |
| `199b860` | BOM قطع غيار قياسية لكل فئة أصل | `asset_category_spare_part/*` (جديد)، `custom_field.json` |

كل commit تم التحقق من صحته syntactically قبل الدفع (`python3 -m
py_compile` لكل ملف Python، `json.load` لكل ملف JSON، `node --check` لكل
ملف JavaScript) — لا يوجد أي commit به خطأ صياغي معروف.

---

## حدود ما تم تنفيذه (Out of Scope)

بالدقة نفسها المطلوبة، هذه هي الأشياء التي **لم** تُنفَّذ عمداً في هذه
المرحلة (وليست نسياناً):

- **لم يُنشأ أي DocType موازٍ** لأي شيء موجود بالفعل (لا فرع، لا مورد، لا
  حساب، لا أصل) — كل تنفيذ إما حقل مخصص، جدول فرعي، أو (لبند SLA فقط)
  DocType جديد لمفهوم لم يكن له أي تمثيل سابق إطلاقاً.
- **لم تُشحَن أي بيانات رئيسية (Master Data) تلقائياً** — لا سياسات SLA
  افتراضية، لا ربط Cost Center↔Branch افتراضي — كلها تنتظر إدخالك اليدوي
  (انظر القسم أعلاه) بدل افتراض قيم قد لا تناسب شركتك.
- **`check_requisition_sla` (سلة موافقات طلب الأصل) لم تُمَس** — مفهوم
  مختلف عن SLA الصيانة، ولم يكن ضمن الفجوات العشر المتفق عليها.
- **لم يتغيّر نموذج الصلاحيات (Permissions) على أي DocType موجود** — كل
  الأدوار الحالية (System Manager، Asset Manager، Asset Finance Manager،
  Asset Technician، Asset User) بقيت كما هي على `Asset Work Order`
  و`Asset Spare Part Request`. الـ DocType الجديدان (SLA Policy، Category
  Spare Part) استُخدِم فيهما نفس نمط الصلاحيات المعمول به في التطبيق.
- **لا وصول مباشر لقاعدة بيانات الإنتاج** — كل ما سبق كود فقط، مدفوع
  للفرع، بانتظار `git pull` + `bench migrate` + `bench restart` والإعداد
  اليدوي المذكور أعلاه على السيرفر الفعلي.
