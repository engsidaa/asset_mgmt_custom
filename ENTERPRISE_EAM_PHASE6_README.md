# Phase 6 — منصة EAM/CMMS مؤسسية: التوثيق الكامل

توثيق تراكمي لمرحلة تطوير "asset_mgmt_custom" إلى منصة إدارة أصول
ومرافق وصيانة على مستوى مؤسسي، وفق الترتيب المعتمد: **التكامل المالي
أولاً**، ثم محرك الموثوقية، ثم السلامة/الامتثال، ثم طبقة الـ API
الخارجية. يُحدَّث هذا الملف مع كل بند يُنجَز — راجع أيضاً `CLAUDE.md`
للمحددات المعمارية الصارمة الحاكمة لكل ما يلي.

**تنويه هام:** قبل أي تنفيذ، جرى مسح كامل لِـ DocTypes الموجودة فعلياً
في هذا التطبيق (`asset_mgmt_custom/asset_mgmt_custom/doctype/`) — تبيَّن
أن **معظم** الأسماء الواردة في مواصفة "EAM المؤسسية" (asset_capex_budget،
asset_lease، asset_warranty_claim، asset_maintenance_sla_policy،
asset_vendor_contract، asset_write_off_request، asset_disposal_execution،
asset_energy_log، asset_fuel_log، asset_safety_inspection،
asset_work_permit، ...) **موجودة بالفعل كسقالة**، لكن أغلبها بمنطق أعمال
شبه معدوم (بعضها ملف `.py` سطر واحد فقط `pass`). التنفيذ هنا يبني **فوق**
هذه السقالة، ولا يُنشئ بنية موازية أو مكررة لها.

---

## القسم أ: التكامل المالي والمحاسبي العميق

### 1) رسملة الـ CWIP إلى أصل ثابت (CWIP → Fixed Asset)

**اكتشاف مهم قبل أي كود:** رسملة الـ CWIP بالكامل — الترحيل التلقائي
من حساب "أصول تحت التنفيذ" إلى حساب "الأصول الثابتة" عند تسليم الأصل —
**مبنية بالفعل في ERPNext الأساسي** ولا تحتاج أي كود مخصص:
- `Asset Category.enable_cwip_accounting` (حقل Core جاهز).
- `Asset Category Account.capital_work_in_progress_account` +
  `fixed_asset_account` (حقول Core جاهزة، تُضبط لكل شركة/فئة).
- `Asset.make_gl_entries()` (كود Core في
  `erpnext/assets/doctype/asset/asset.py`) يُستدعى تلقائياً عند تسليم
  (submit) الأصل، ويُنشئ قيد التحويل (دائن CWIP / مدين الأصل الثابت)
  بالقيمة الكاملة، مستخدماً `self.cost_center` مباشرة.
- عند شراء صنف مُعلَّم `is_fixed_asset=1` عبر Purchase Receipt/Purchase
  Invoice، ينشئ ERPNext تلقائياً سجل **Asset** كمسودة
  (`buying_controller.make_asset()`) — لا حاجة لمستند "شهادة استلام"
  منفصل، فالـ Purchase Receipt نفسه هو لحظة الاستلام.

**بناء كود مخصص لتكرار هذا كان سيُصادم مباشرة مع منطق Core (قيود مزدوجة
محتملة)** — لذا الفجوة الحقيقية الوحيدة التي عُولجت هنا:

- **الفجوة**: `make_asset()` الأساسي يضبط `location` فقط على الأصل
  الجديد، ولا يعرف شيئاً عن `custom_branch`/`cost_center` الخاصين بهذا
  التطبيق — فكان قيد الرسملة التلقائي (الذي يستخدم `self.cost_center`
  كما ذُكر) يُرحَّل بمركز تكلفة فارغ لأي أصل يُشترى عبر هذه الآلية.
  **الإصلاح**: `overrides/asset.py` → `_inherit_branch_and_cost_center()`
  (تُستدعى من `validate()`) تفرض هرمية `Company -> Branch -> Location ->
  Asset`: تشتق `custom_branch` من `Branch.custom_default_location`
  (مطابقة عكسية بموقع الأصل)، وتشتق `cost_center` من
  `Location.custom_cost_center` أو مركز تكلفة الفرع، تلقائياً، دون إدخال
  يدوي.
- **سلسلة التتبع الكاملة** (Asset Requisition → Material Request →
  Purchase Receipt → Asset) كانت **مقطوعة فعلياً وبصمت**: الحقل
  `Material Request.custom_source_asset_requisition` الذي كان الكود
  القديم في `asset_requisition.py::create_purchase_requisition()`
  يحاول ضبطه، **لم يكن موجوداً إطلاقاً** كحقل مخصص — الشرط الدفاعي
  `if frappe.db.exists("Custom Field", ...)` كان يُرجِع `False` دائماً،
  فالحقل يُترك `None` دائماً. تم:
  1. إضافة `Material Request.custom_source_asset_requisition` (Link)
     فعلياً في `fixtures/custom_field.json`.
  2. تبسيط `create_purchase_requisition()` لضبطه مباشرة.
  3. إضافة `Asset.custom_source_requisition` و
     `Asset Requisition.linked_asset` (رابطان في الاتجاهين)، وربطهما
     تلقائياً في `overrides/asset.py::_link_source_requisition()`
     (تُستدعى من `after_insert()`) بتتبُّع
     `Purchase Receipt Item.material_request` للخلف.

**الإعداد المطلوب منك** (بيانات فقط، لا كود): فعِّل
`Enable Capital Work in Progress Accounting` على فئات الأصول الرأسمالية
(معدات كبيرة، مطابخ، تبريد...)، واضبط `Capital Work in Progress Account`
و`Fixed Asset Account` على `Asset Category Account` لكل شركة.

**اختبار:** أنشئ Purchase Receipt لصنف `is_fixed_asset=1` مرتبط بـ
Material Request صادر من Asset Requisition ← تأكد أن الأصل المُنشأ
تلقائياً يحمل `custom_branch`/`cost_center` صحيحين فوراً، وأن
`custom_source_requisition` يشير لطلب الأصل الأصلي وأن
`Asset Requisition.linked_asset` يشير للأصل ← فعِّل CWIP على الفئة وحدِّد
الحسابات ← سلِّم (submit) الأصل ← تأكد من ظهور قيد يومية تلقائي حقيقي
(Core) بمركز التكلفة الصحيح.

---

### 2) الصرف المخزني الآلي لقطع الغيار مع أوامر العمل

**قبل هذا البند:** كانت "Asset Spare Part Request" و"Asset Work Order"
منفصلتين تماماً — `spare_parts_cost` على أمر العمل رقم يُكتب يدوياً بلا
أي أثر مخزني حقيقي، بينما صرف قطعة الغيار الفعلي (Stock Entry حقيقية،
موجود مسبقاً في `issue_spare_part()`) كان إجراءً منفصلاً بالكامل لا
يعرف عن أمر العمل شيئاً.

**التعديل:**
- حقل جديد `Asset Spare Part Request.asset_work_order` (Link) + حقلا
  `batch_no`/`serial_no` اختياريان يُمرَّران فعلياً إلى بند حركة المخزون
  عند الصرف.
- `Asset Work Order.complete_work_order()` يستدعي
  `_auto_issue_linked_spare_parts()`: يصرف تلقائياً (Stock Entry حقيقية)
  كل طلب قطعة غيار مرتبط بهذا الأمر وبحالة "Approved" لم يُصرَف بعد
  (يُعيد استخدام `issue_spare_part()` الموجودة، بلا تكرار منطق)، ثم
  يُحدِّث `spare_parts_cost` من القيمة الفعلية المُقيَّمة
  (`Stock Entry.total_outgoing_value`) — الحقل أصبح **للقراءة فقط**، لم
  يعد يُكتب يدوياً.
- `on_cancel()` يستدعي `_cancel_linked_spare_part_requests()`: يُلغي كل
  طلب قطعة غيار صُرف عبر هذا الأمر (يُلغي حركة المخزون ويُعيد الكمية —
  عبر `Asset Spare Part Request.on_cancel()` الموجودة أصلاً).
- **تصحيح خطر ازدواج محاسبي محتمل**: بما أن Stock Entry الآن تُنشئ قيدها
  المحاسبي التلقائي الخاص بها (مصروف الصيانة/قيمة المخزون) عند الصرف،
  توقف `_post_maintenance_cost_gl_entry()` عن تضمين `spare_parts_cost`
  ضمن قيد اليومية الخاص بأمر العمل — أصبح `total_cost = labor_cost`
  فقط (أو `actual_cost` كتجاوز يدوي صريح المسؤولية عن عدم تكراره).

**اختبار:** أنشئ Asset Spare Part Request واربطه بأمر عمل مفتوح، اعتمده
(Approved) ← أتمم أمر العمل ← تأكد من: (1) إنشاء Stock Entry حقيقية
وخصم الكمية من المستودع، (2) `spare_parts_cost` على أمر العمل يعكس
القيمة الفعلية وليس تقديراً، (3) القيد اليومي لا يحمل نفس تكلفة قطع
الغيار مرة ثانية. ثم ألغِ أمر عمل آخر بطلب قطعة غيار مصروفة بالفعل ←
تأكد من إلغاء حركة المخزون واسترجاع الكمية.

---

### 3) عمرة كبرى مُرسملة (CapEx) مقابل صيانة إيرادية (OpEx)

**اكتشاف مهم قبل أي كود:** هذا البند **موجود بالفعل بالكامل وبشكل
صحيح**، لكن على مستند **Asset Repair** (المستند الرسمي لتوثيق الإصلاح)
وليس Asset Work Order (تذكرة الصيانة اليومية/التوزيع/SLA):
`overrides/asset_repair.py` يحتوي فعلياً على التصنيف الآلي حسب حد
الرسملة (`custom_capitalization_threshold` على فئة الأصل)، وفرض إدخال
"الزيادة في العمر الإنتاجي" لأي إصلاح مُرسمَل، والتوجيه المحاسبي الصحيح
(CapEx → `custom_capital_maintenance_wip_account` ثم للأصل، OpEx → مصروف
الصيانة). إعادة بناء كل هذا داخل Asset Work Order كان سيُكرِّر/يُصادم
مباشرة مع منطق موجود وصحيح بالفعل.

**الفجوة الحقيقية:** لا يوجد أي جسر بين الاثنين — فني يتعامل مع اليوم
عبر Asset Work Order (ولا Kanban ولا SLA ولا الويزارد الجديد أياً منها
مبني حول Asset Repair) ليس عنده أي طريقة لتصعيد "الأمر ده طلع عمرة كبرى"
بدون ترك أمر العمل وإنشاء Asset Repair يدوياً من الصفر، فيفقد كل السياق
(الفرع، SLA، الفني المُسنَد، صور العطل...).

**التعديل:** حقل جديد `Asset Work Order.cost_classification`
("Revenue Expense" الافتراضي / "Capitalized Overhaul") + `increase_in_
asset_life_months`. عند إتمام أمر مُصنَّف "Capitalized Overhaul"،
`complete_work_order()` يستدعي `_escalate_to_asset_repair()`: تُنشئ
سجل **Asset Repair** حقيقياً (بيانات مأخوذة من أمر العمل: الأصل، تاريخ
الإتمام، التكلفة، اسم الفني، الملاحظات) بـ `capitalize_repair_cost=1`،
وتُسلِّمه — فتتكفل منطق Asset Repair الموجود فعلياً بكل شيء (رفع القيمة
الدفترية، تمديد العمر، القيد المحاسبي CapEx). أمر العمل نفسه **لا** ينشئ
قيده اليومي المعتاد في هذه الحالة (تفادي ازدواج محاسبي)، ويُلغى الاثنان
معاً تلقائياً عند إلغاء أمر العمل. وُصِّل هذا أيضاً في ويزارد "إتمام/رفض
أمر عمل" (خطوة جديدة: تصنيف التكلفة + الزيادة في العمر الإنتاجي عند
الاختيار).

**اختبار:** أتمم أمر عمل عادي ← تأكد من عدم ظهور أي تغيير (نفس سلوك
"Financial 2" تماماً). أتمم أمر عمل آخر مع اختيار "Capitalized Overhaul"
وزيادة عمر 24 شهراً ← تأكد من إنشاء Asset Repair مُسلَّم ومُرسمَل مرتبط،
وأن القيمة الدفترية للأصل ارتفعت وجدول الإهلاك أُعيد جدولته، وأن أمر
العمل **لم** يُنشئ قيد يومية خاصاً به. ألغِ ذلك الأمر ← تأكد من إلغاء
Asset Repair المرتبط أيضاً.

---

### 4) محاسبة الشطب والتكهين

**تدقيق شامل قبل أي تعديل** لِـ 3 مستندات مرتبطة بالتخلص من الأصول:
`Asset Write-off Request` (منطق موجود، فيه خلل محاسبي حقيقي)،
`Asset Disposal Execution` (لا يوجد أي أثر محاسبي إطلاقاً قبل هذا
البند)، `Asset Disposal Certificate` (وثيقة/سجل رسمي فقط، لا يُفترض
أن يكون له أي أثر محاسبي — تُرك كما هو).

**خلل محاسبي حقيقي في `Asset Write-off Request.create_journal_entry()`:**
القيد كان يُنشئ طرفين فقط: مدين حساب الشطب / دائن حساب الأصول الثابتة،
كلاهما **بالقيمة الدفترية بعد الإهلاك فقط** — وليس سعر الشراء الكامل.
هذا يعني أن حساب "الأصول الثابتة" (المُسجَّل دائماً بسعر الشراء الإجمالي)
كان يبقى برصيد متبقٍ = مجمع الإهلاك **إلى الأبد** لهذا الأصل تحديداً،
لأن مجمع الإهلاك نفسه لم يكن يُقفَل أبداً بأي قيد. **الإصلاح:** قيد من 3
أطراف الآن: مدين مجمع الإهلاك (بكامل قيمته) + مدين حساب الشطب (بالقيمة
الدفترية فقط، أي الخسارة الحقيقية) + دائن حساب الأصول الثابتة (بسعر
الشراء الكامل) — يُقفل الحسابين الحقيقيين بالكامل. كذلك: حساب القيمة
الدفترية التلقائي (لو لم يُدخِلها المستخدم يدوياً) أصبح يستخدم
`Asset.get_value_after_depreciation()` (الدالة الأساسية الموثوقة في
Core) بدل استعلام SQL كان يُرجِع دائماً 0 فعلياً (خطأ في مطابقة الجدول
الفرعي). وأصبح المستند بعد الترحيل يضبط `Asset.status = "Scrapped"` +
`disposal_date` (لم يكن يفعل ذلك سابقاً — الأصل كان يبقى "Submitted" في
كل تقرير رغم شطبه فعلياً)، ويرفض الترحيل صراحة لو الأصل مُتخلَّص منه
بالفعل (منع ازدواج).

**فجوة كاملة في `Asset Disposal Execution`:** كان تسليمه يُسجِّل الحدث
فقط (طريقة/تاريخ التخلص) **بدون أي أثر محاسبي على الإطلاق** — لا قيد،
ولا حتى تحديث حالة الأصل. **الإصلاح:** يستدعي الآليات الأساسية الحقيقية
في ERPNext (بدل اختراع محاسبة تخلص مكررة قد تصادمها):
- `Scrapped`/`Destroyed`/`Donated` (بلا قيمة بيع حقيقية): يستدعي
  `erpnext...depreciation.scrap_asset()` مباشرة — نفس دالة زر "Scrap
  Asset" على نموذج الأصل (إهلاك نهائي + قيد إقفال صحيح + تحديث الحالة،
  كل ذلك Core بالكامل).
- `Sold`/`Auctioned` (قيمة بيع فعلية): يُنشئ **مسودة** فاتورة بيع عبر
  `erpnext...asset.make_sales_invoice()` (نفس دالة زر "Sell Asset")،
  ويتركها لفريق المالية لتحديد العميل/الضريبة وتسليمها يدوياً — عمداً
  بدون تسليم تلقائي، لأن اختيار العميل قرار بشري.
- الإلغاء (`on_cancel`) يعكس ما سبق: يستدعي `restore_asset()` لو كان
  Scrap، أو يحذف مسودة فاتورة البيع لو لم تُسلَّم بعد (ويحذّر بدل الحذف
  التلقائي لو كانت مُسلَّمة بالفعل — عكس بيع فعلي قرار بشري أيضاً).

**اختبار:** أنشئ Asset Write-off Request لأصل له إهلاك متراكم فعلي ←
اعتمده ← أنشئ القيد ← تأكد من 3 أطراف صحيحة وأن رصيد حساب الأصول الثابتة
وحساب مجمع الإهلاك لهذا الأصل أصبحا صفراً، وأن حالة الأصل "Scrapped".
ثم منفصلاً: أنشئ Asset Disposal Execution بأسلوب "Scrapped" لأصل آخر ←
سلِّمه ← تأكد من ظهور قيد Scrap تلقائي (Core) وتحديث حالة الأصل. جرِّب
أيضاً أسلوب "Sold" بقيمة بيع ← تأكد من ظهور فاتورة بيع مسودة بالقيمة
الصحيحة تنتظر المراجعة.

---

### 5) محاسبة الإيجار وفق IFRS 16 (أصل حق الاستخدام + التزام الإيجار)

**اكتشاف مهم قبل أي كود:** مستند `Asset Lease` الموجود بالفعل يُمثِّل
حالة **عكس** ما يحتاجه IFRS 16: حقوله (`lessee_name`, `lessee_type`:
Internal Branch/External Third Party, `monthly_rent` كإيراد) تصف الشركة
وهي **المؤجِّر** (Lessor) — تُؤجِّر أصلاً تملكه بالفعل لطرف آخر. أما
IFRS 16 (أصل حق استخدام + التزام إيجار) ينطبق فقط عندما تكون الشركة هي
**المستأجر** (Lessee) — تستأجر معدة **لا تملكها**. تطبيق منطق IFRS 16
مباشرة على السلوك القديم كان سيُطبَّق بالعكس تماماً.

**الحل:** حقل جديد `lease_direction` ("We are the Lessor" الافتراضي
—يحافظ على كل السلوك القديم دون أي تغيير— / "We are the Lessee")، وقسم
جديد كامل يظهر فقط عند اختيار "We are the Lessee":
- `discount_rate` (معدل الاقتراض الإضافي للمستأجر).
- 6 حسابات مخصصة لهذا العقد: ROU Asset، مجمع إهلاك ROU، التزام الإيجار،
  مصروف الفائدة، مصروف إهلاك ROU، حساب الدفع.
- عند التسليم: `_recognize_ifrs16_lease()` تحسب القيمة الحالية
  (Present Value) لإجمالي دفعات الإيجار المتبقية بمعدل الخصم (صيغة
  Ordinary Annuity المعيارية)، وتُنشئ قيد اعتراف ابتدائي (مدين ROU Asset
  / دائن التزام الإيجار) بنفس القيمة على الطرفين.
- شهرياً (مهمة مجدولة يومية جديدة `process_lease_amortization` في
  `tasks.py`، تُرحِّل كل عقد عبر `frappe.enqueue` منفصل): فائدة الشهر =
  رصيد الالتزام المتبقي × المعدل الشهري (طريقة الفائدة الفعلية)، أصل
  الالتزام = القسط الثابت - الفائدة، وإهلاك ROU بالقسط الثابت على كامل
  مدة العقد (منفصل تماماً عن جدول الفائدة، كما يقتضي IFRS 16) — قيد
  يومية واحد بكل هذا مجمَّعاً، ويُسجَّل صف في جدول `lease_amortization_
  schedule` الفرعي على المستند نفسه (شفاف بالكامل للمراجعة المالية).
- الإلغاء يعكس كل القيود (الاعتراف الابتدائي + كل أقساط الإطفاء
  المُرحَّلة).

**لماذا لم تُبنَ كـ Asset ERPNext حقيقي:** بناء أصل حق الاستخدام كسجل
"Asset" حقيقي في ERPNext كان سيتطلب إعداد Finance Books/Asset Category
كاملة بشكل برمجي (خطر أخطاء أعلى بكثير)، بينما ERPNext لا يقدم أي دعم
IFRS 16 أصلي أساساً (لا شيء يُكرَّر هنا من Core) — فالمحاسبة عبر قيود
يومية مباشرة بحسابات مخصصة على العقد نفسه هي الطريقة الأبسط والأصح
لتطبيق عملي غير مُقدَّم أصلاً من ERPNext.

**اختبار:** أنشئ Asset Lease بـ `lease_direction = We are the Lessee`،
إيجار شهري ومعدل خصم ومدة 12 شهراً وحسابات محددة ← سلِّمه ← تأكد من
قيد اعتراف ابتدائي صحيح (ROU = التزام) وتطابق `initial_rou_value` مع
حساب PV يدوي. غيِّر `next_amortization_date` ليدوياً للماضي (لاختبار
فوري بدل انتظار شهر) ونفِّذ `process_lease_amortization()` من
`bench execute` ← تأكد من ظهور قيد شهري صحيح وانخفاض `lease_liability_
balance` وإضافة صف في الجدول الفرعي. ألغِ العقد ← تأكد من إلغاء كل
القيود المرتبطة.

---

### 6) محرك احتساب تكلفة الملكية الإجمالية (TCO) اللحظي

**قبل هذا البند:** تقرير `Asset Total Cost of Ownership` (موجود بالفعل
من مرحلة سابقة) شامل وصحيح فعلياً — يجمع سعر الشراء + الإهلاك + الصيانة
+ التأمين + عقود الموردين + الإيجار + التراخيص + قطع الغيار + خسارة
الشطب + الطاقة/الوقود، ويحسب توصية إصلاح/استبدال حسب عتبة كل فئة. لكنه
يُعاد حسابه بالكامل (JOINs عبر 8 مصادر) في كل مرة يُفتح فيها مباشرة —
غير مناسب للعرض المتكرر في لوحات تحكم/فلاتر/واجهة API الموبايل لأسطول
أصول كبير.

**التعديل (بلا تكرار لأي منطق موجود):**
- مهمة أسبوعية جديدة `tasks.refresh_tco_cache()` تستدعي `get_data()`
  **من نفس ملف التقرير مباشرة** (نفس الحساب بالضبط، صفر تكرار)، وتُخزِّن
  الناتج على حقول مخصصة جديدة على Asset نفسه: `custom_tco`,
  `custom_annual_tco`, `custom_tco_recommendation`,
  `custom_tco_is_outlier`, `custom_tco_last_computed` — متاحة الآن فوراً
  لأي شاشة/فلتر/تقرير آخر (وواجهة API الموبايل مستقبلاً) دون إعادة حساب.
- **مقارنة الأصول المتماثلة** (`_flag_tco_outliers` في التقرير نفسه):
  لكل فئة أصل بها 3 أصول أو أكثر (عيّنة ذات دلالة)، يُحسَب متوسط
  `annual_tco` للفئة، ويُعلَّم أي أصل تتجاوز تكلفته السنوية 150% من
  متوسط نظرائه كـ Outlier — هذا هو "تحديد الأصول المتهالكة التي تستنزف
  الأرباح" المطلوب صراحة، ويظهر في التقرير نفسه فوراً (وليس فقط في
  الحقول المُخزَّنة أسبوعياً).
- **توزيع على مستوى الفروع**: فلتر/عمود `Branch` جديد على التقرير
  (`custom_branch` على Asset، الموجود بالفعل — لم يكن مستخدَماً في هذا
  التقرير تحديداً).
- Number Card جديد "TCO Outlier Assets" على workspace "إدارة الأصول".

**اختبار:** نفِّذ `refresh_tco_cache()` يدوياً عبر `bench execute` ←
تأكد من امتلاء الحقول الجديدة على عدة أصول. افتح التقرير وفلتر بفرع
معيّن ← تأكد من ظهور أصول هذا الفرع فقط. لو فئة أصل بها 3 أصول فأكثر
بفروق تكلفة واضحة، تأكد من تعليم الأعلى تكلفة كـ Outlier في عمود
`is_tco_outlier` وظهور Number Card بعدد مطابق.

---

**بهذا تكتمل كل بنود القسم أ (التكامل المالي)، الستة بالكامل.**

## القسم ب: محرك الصيانة والموثوقية المتقدم

### 1) مصفوفة ISO 14224 (Problem / Cause / Remedy Code)

**قبل هذا البند:** `Asset Failure Analysis` كان يحتوي على `failure_mode`
(Select — تصنيف واحد فقط يعمل عملياً كـ "رمز سبب") بجانب `symptom` و
`corrective_action` كنص حر بالكامل — لا يوجد تصنيف مُقنَّن مستقل لمظهر
العطل الخارجي (Problem) ولا للإجراء التصحيحي (Remedy)، فلا يمكن عمل
إحصاء/مصفوفة FMEA موثوقة (مثال: "أي رموز مشكلة تقترن غالباً بأي إجراء
تصحيحي لنفس السبب"). كذلك: لا يوجد أي ربط بين `Asset Work Order` (تذكرة
الصيانة اليومية) و`Asset Failure Analysis` إطلاقاً.

**التعديل:**
- حقلان جديدان: `problem_code` (مستوى 1 — مظهر خارجي: توقف كامل/أداء
  ضعيف/ضوضاء/تسريب/ارتفاع حرارة/رائحة احتراق/كسر مادي...) و`remedy_code`
  (مستوى 3 — تصنيف الإجراء: استبدال قطعة/إصلاح وتصنيع/تنظيف وتشحيم/
  معايرة/تحديث برمجي/استبدال كامل/لا حاجة لإجراء). `failure_mode`
  الموجود أُعيد تسميته مفاهيمياً (Label فقط، بدون تغيير fieldname أو
  قيمه الحالية) إلى "رمز السبب الجذري (Cause Code)" — المستوى 2. الحقول
  النصية الحرة (`symptom`, `corrective_action`) بقيت كتفصيل تكميلي
  اختياري، لم تُحذف.
- `Asset Work Order.failure_analysis` (Link) + `Asset Failure
  Analysis.work_order` (Link عكسي) — يتزامنان تلقائياً في أي اتجاه
  (`_sync_work_order_link`)، مع زر إنشاء/عرض سريع على نموذج أمر العمل.
- `get_suggested_remedies()` (محرك التوصيات من Phase 4) أصبح يقارن
  بتركيبة (`problem_code` + `failure_mode`) معاً بدل `failure_mode`
  وحده، ويُرجِع توزيع `remedy_code` الأكثر تكراراً — إحصاء FMEA حقيقي
  الآن، وليس تجميع نص حر تقريبي.

**اختبار:** أنشئ Asset Work Order لعطل ما ← من زر "تحليل سبب العطل
(ISO 14224)" أنشئ تحليلاً مرتبطاً ← اختر رمز مشكلة ورمز سبب ورمز إجراء
← احفظ ← تأكد أن `Asset Work Order.failure_analysis` امتلأ تلقائياً.
سجِّل عدة تحليلات بنفس (problem_code + failure_mode) وremedy_code
مختلفة ← اضغط "اقتراحات حلول سابقة" من تحليل جديد بنفس التركيبة ← تأكد
من ظهور التوزيع الصحيح مرتباً بالتكرار.

---

### 2) الصيانة مزدوجة المحفز والمتداخلة (Dual-Trigger & Nested PM)

**قبل هذا البند:** المحفز الزمني (next_due_date) للصيانة الوقائية موجود
ويعمل بالفعل من مرحلة سابقة (`send_maintenance_due_alerts` → ينشئ Asset
Work Order تلقائياً عند الاستحقاق). لكن لا يوجد أي محفز بقراءة العداد
(ساعات تشغيل/كيلومترات/إنتاج)، ولا أي منع لتكرار فتح أوامر عمل منفصلة
لعدة مستويات صيانة متداخلة (شهري + ربع سنوي) لنفس الأصل في نفس اليوم.

**التعديل:**
- 3 حقول جديدة على `Asset Maintenance Task` (Core، عبر fixtures):
  `custom_meter_trigger_type` (نوع العداد)، `custom_meter_trigger_value`
  (حد الفرق التراكمي)، `custom_last_triggered_meter_value` (القراءة
  المرجعية عند آخر استحقاق). مهمة جديدة يومية `check_meter_triggered_pm`:
  لكل بند مُهيَّأ بمحفز عداد، تُقارَن آخر قراءة فعلية (`Asset Meter
  Reading.current_reading`) بالقراءة المرجعية — لو تجاوز الفرق الحدَّ،
  يُنشأ أمر عمل بنفس آلية `_auto_create_work_order_from_task` (idempotent
  بلا تكرار منطق)، وتتقدَّم القراءة المرجعية فقط بعد إنشاء أمر فعلي.
- **الصيانة المتداخلة**: `PM_PERIODICITY_RANK` يُرتِّب `periodicity`
  (الحقل الأساسي الموجود بالفعل على Asset Maintenance Task — لا حاجة
  لحقل "مستوى" جديد) من الأقصر (يومي) للأطول (3 سنوات).
  `_higher_level_pm_due_same_day()` تتحقق: لو بند صيانة **أعلى مستوى**
  لنفس الأصل مُستحَق في نفس اليوم، يُتخطَّى إنشاء أمر عمل للبند الأدنى
  (الزيارة الأشمل تُغطيه عملياً) — يُطبَّق هذا الفحص في كلا المحفزين
  (الزمني والعدادي) عبر نفس الدالة المشتركة.

**اختبار:** أضف محفز عداد على بند صيانة (مثال: كل 500 ساعة تشغيل) ←
سجِّل قراءات Asset Meter Reading حتى يتجاوز الفرق 500 ← نفِّذ
`check_meter_triggered_pm()` يدوياً ← تأكد من إنشاء أمر عمل وتقدُّم
القراءة المرجعية. لأصل بمهمتين (شهري + ربع سنوي) بنفس next_due_date ←
تأكد من إنشاء أمر عمل واحد فقط (للربع سنوي)، وتخطي الشهري.

---

### 3) مؤشر صحة الأصل (AHI) والتنبؤ بالعمر المتبقي (RUL)

مهمة أسبوعية جديدة `refresh_asset_health_index()` — بنفس أسلوب الخصم
الشفاف بسقف أقصى لكل عامل المُستخدَم فعلياً في تقرير Branch Health Score
(وليس معادلة مغلقة):
```
AHI = 100
  − min((100 − نسبة العمر المتبقي) × 0.30, 30)
  − min((100 − نتيجة آخر تقييم حالة) × 0.30, 30)
  − min(عدد الأعطال آخر 12 شهراً × 10, 25)
  − min(انحراف درجة حرارة حالي × 15, 15)
```
كل عامل مُعاد استخدامه من بيانات موجودة فعلياً — **صفر تكرار حسابي**:
- العمر المتبقي: من `Asset Finance Book.total_number_of_depreciations`
  مقابل `total_number_of_booked_depreciations` (نفس ما يعتمده
  `Asset.get_value_after_depreciation()` أساساً) — وليس حساب تاريخ يدوي.
  العمر المتبقي بالأشهر (RUL) = الفروقات المتبقية × دورية الإهلاك.
- آخر تقييم حالة: أحدث `Asset Condition Assessment.overall_condition`
  (Excellent→100 ... Critical→0؛ محايد 70 لو لا يوجد تقييم بعد).
- تكرار الأعطال: عدد `Asset Failure Analysis` لهذا الأصل آخر 365 يوماً.
- انحراف الحرارة: يعيد استخدام حدود `Asset Category.custom_min/max_
  safe_temperature` (ميزة سلسلة التبريد من Phase 4) مقابل آخر قراءة.

**التنبيه: لو AHI < 40%** ولا توجد بالفعل مسودة/خطة معتمدة سابقة لنفس
الأصل، تُنشأ تلقائياً **مسودة** `Asset Replacement Plan` (موجود بالفعل
كسقالة كاملة الحقول تقريباً من مرحلة سابقة — `capex_budget`,
`remaining_life_years`, `approval_status` افتراضي "مسودة") — **لن تُعتمَد
تلقائياً أبداً**، تحتاج مراجعة بشرية وربط بميزانية CapEx فعلية.
Number Card جديد "Low AHI Assets" على workspace "إدارة الأصول".

**اختبار:** نفِّذ `refresh_asset_health_index()` يدوياً ← تأكد من امتلاء
`custom_ahi_score`/`custom_rul_months` على عدة أصول. لأصل بتاريخ شراء
قديم جداً وأعطال متكررة، تأكد من AHI منخفض وظهور مسودة Asset Replacement
Plan تلقائياً مربوطة عبر `custom_replacement_plan`، وأن تشغيل المهمة
مرة أخرى **لا** ينشئ مسودة مكررة.

---

### 4) إدارة ومحاسبة قطع الغيار الدوارة (Rotable Spares)

**التعديل:** `Asset Spare Part.is_rotable` (Check) + `core_return_
warehouse` (مستودع ورشة الصيانة المركزية) + `pending_refurbishment_qty`
(للقراءة فقط). دورة الحياة الكاملة:

1. **الصرف والاستلام معاً**: `Asset Spare Part Request` حقل جديد
   `failed_unit_serial_no` — عند تعبئته مع قطعة دوارة، `issue_spare_part()`
   (بلا تعديل في مسارها الأصلي) تستدعي `_receive_failed_core_unit()`:
   Stock Entry حقيقية من نوع Material Receipt تستلم الوحدة التالفة في
   مستودع الاستلام **بقيمة صفر مبدئياً** (تكلفتها الحقيقية تتحدد فقط بعد
   التجديد)، وترفع `pending_refurbishment_qty`. الإلغاء يعكس الاثنين معاً
   (الصرف والاستلام).
2. **إتمام التجديد**: `asset_spare_part.complete_refurbishment()` (زر
   جديد "إتمام تجديد وحدة" يظهر فقط عند وجود وحدات بانتظار التجديد) —
   Stock Entry من نوع Material Transfer تنقل الوحدة من مستودع الاستلام
   إلى المستودع الرئيسي **بتكلفة التجديد الفعلية** (وليس صفراً، وليست
   قطعة جديدة مُشتراة) — تُعيد القطعة للخدمة بقيمة حقيقية تعكس تكلفة
   التجديد فقط، لا تكلفة شراء كاملة جديدة.

**لماذا Stock Entry وليس Asset Work Order:** قطعة الغيار ليست "أصلاً"
مستقلاً في ERPNext (لا سجل Asset لها)، فربط دورتها بمستند Asset Work
Order (مركزه الأساسي أصل ثابت كامل، SLA، فني مُسنَد...) كان سيفرض بنية
لا تناسبها. حركات المخزون الحقيقية (مع أرقام تسلسلية) هي التمثيل الصحيح
لموقع/حالة كل وحدة فعلية.

**اختبار:** فعِّل `is_rotable` على قطعة غيار وحدِّد مستودع استلام ← أنشئ
Asset Spare Part Request بها مع رقم تسلسلي للوحدة التالفة ← اعتمده
وصرِّفه ← تأكد من حركتين حقيقيتين (صرف الجديدة + استلام التالفة)
وارتفاع `pending_refurbishment_qty`. اضغط "إتمام تجديد وحدة" بتكلفة
تجديد ← تأكد من نقل الوحدة للمستودع الرئيسي بالتكلفة الصحيحة وانخفاض
`pending_refurbishment_qty` وارتفاع `quantity`.

---

### 5) مطابقة الضمان وغرامات عقود الصيانة تلقائياً

**بوابة الضمان:** `Asset Spare Part Request.validate()` تتحقق: لو الأصل
`custom_under_warranty=1` (حقل موجود بالفعل من Phase 4)، تُرفَض الطلب
فوراً — قطع الغيار المفروض تأتي من المورِّد عبر `Asset Warranty Claim`
مجاناً، وليس من مخزوننا الخاص. الاستثناء الوحيد: أمر عمل مرتبط صراحة
بمطالبة ضمان (`Asset Work Order.warranty_claim` — حقل جديد) لتغطية قطع
تكميلية غير مشمولة بالضمان نفسه فعلاً.

**غرامات SLA التعاقدية:** حقلان جديدان على `Asset Vendor Contract`:
`penalty_per_sla_breach` (المبلغ) و`penalty_account` (حساب دائن). عند
أول خرق SLA لأمر عمل (داخل `check_overdue_work_orders` الموجودة فعلياً
— **فقط أول مرة يتحول فيها sla_breached من 0 إلى 1، لا في كل تكرار
يومي لاحق**)، تبحث `_find_matching_vendor_contract` عن أول عقد مورِّد
نشط يغطي هذا الأصل أو فئته وله جزاء مُحدَّد (نفس منطق مطابقة العقود
المُستخدَم فعلياً في تقرير TCO، بلا تكرار)، وتُنشئ `_propose_sla_penalty`
**مسودة** Journal Entry (مدين حساب مستحقات المورِّد Payable/party_type=
Supplier، دائن حساب الجزاء) — **بلا تسليم (`insert` فقط، لا `submit`)**:
اقتراح صريح يحتاج مراجعة واعتماد المحاسب، تماماً مثل كل الاقتراحات
التلقائية الأخرى في هذه المرحلة (Asset Replacement Plan، فاتورة بيع
التخلص). حقلا `vendor_contract`/`penalty_journal_entry` جديدان على
Asset Work Order للتتبع + زر "مراجعة مسودة جزاء SLA".

**اختبار:** فعِّل `custom_under_warranty` على أصل ← حاول إنشاء Asset
Spare Part Request له ← تأكد من الرفض برسالة واضحة. اربط أمر عمل بمطالبة
ضمان ← اربط طلب قطعة غيار بنفس الأمر ← تأكد من قبوله هذه المرة. أنشئ
Asset Vendor Contract نشطاً بجزاء SLA محدد يغطي فئة/أصل معين ← اجعل أمر
عمل على نفس الأصل يتجاوز موعد الحل ← نفِّذ `check_overdue_work_orders()`
← تأكد من إنشاء مسودة Journal Entry بالمبلغ الصحيح مربوطة على أمر العمل
← نفِّذها مرة أخرى ← تأكد من **عدم** إنشاء مسودة ثانية.

---

**بهذا يكتمل القسم ب (محرك الصيانة والموثوقية المتقدم) بالكامل —
5 بنود.**

## القسم ج: السلامة المتقدمة والامتثال المؤسسي

### 1) إجراءات العزل والسلامة المهنية (Lockout / Tagout)

**التعديل:** `Asset Work Permit` (موجود بالفعل، يشمل نوع "أعمال كهربائية"
ورابط `work_order`) — قسم جديد كامل: `requires_loto` (Check، يقرره مسؤول
السلامة يدوياً لكل تصريح حسب الخطورة الفعلية، بدل قائمة أنواع تصاريح
ثابتة) + 6 خطوات عزل (تحديد مصدر الطاقة، الفصل، تركيب الأقفال، لافتات
التحذير، التحقق الفعلي من صفر طاقة، معدات الحماية الشخصية) + `loto_
verified`/`loto_verified_by`/`loto_verified_on` (للقراءة فقط، تُضبط حصراً
عبر الدالة المُصرَّح بها `complete_loto_checklist()` — "التوقيع
الإلكتروني" هنا يعني تسجيل هوية الموقِّع + الوقت فعلياً عبر استدعاء
صريح، بنفس نمط `mark_coded()` الموجود مسبقاً في التطبيق، وليس مجرد
Checkbox عادي أي أحد يقدر يعلِّمه).

**البوابة الحقيقية:** `Asset Work Order.before_submit()` (جديد) — تسليم
أمر العمل هو اللحظة التي يتحول فيها تلقائياً لحالة "قيد التنفيذ"
(`on_submit`). لو الأمر مرتبط بتصريح يتطلب عزل طاقة ولم يُوقَّع اكتماله
بعد، **يُمنَع التسليم بالكامل** — لا يمكن بدء العمل الفعلي على معدة
خطرة بدون توثيق العزل أولاً.

**اختبار:** أنشئ Asset Work Permit بـ `requires_loto=1` ← اربطه بأمر عمل
← حاول تسليم أمر العمل قبل توقيع قائمة العزل ← تأكد من الرفض برسالة
واضحة. أكمل كل خطوات العزل على التصريح واضغط "توقيع اكتمال عزل الطاقة"
← تأكد من تسجيل الموقِّع والوقت ← سلِّم أمر العمل مرة أخرى ← تأكد من
نجاحه هذه المرة.

---

### 2) قوائم الفحص الديناميكية: توثيق إلزامي + تصعيد تلقائي

**تحديد النطاق:** `Asset Safety Inspection` (له بالفعل بنية بند/نتيجة
Pass/Fail/N/A حقيقية عبر `Asset Safety Inspection Item`) هو ما ينطبق
عليه هذا البند فعلياً. `Asset Cleaning Schedule` (الآخر المذكور في
المواصفة) جدول مواعيد بسيط (Scheduled/Completed/Missed) **بلا بنية بند/
نتيجة على الإطلاق** — فرض نفس منطق "الفشل" عليه كان يتطلب إعادة تصميم
مستقلة أكبر من نطاق هذا الإصلاح، فتُرِك كما هو.

**التعديل على Asset Safety Inspection Item:**
- `photo` (Attach Image) — **إلزامية** الآن عند `result = "Fail"`
  (`_require_photo_on_fail`، تمنع الحفظ بالكامل بدونها لأي بند فاشل).
- `corrective_work_order` (Link، للقراءة فقط) — `on_update()` على
  المستند الأب يُنشئ تلقائياً **Asset Work Order بأولوية "حرج"** واحداً
  لكل بند فاشل (وليس واحداً للتفتيش كله، لأن كل بند فاشل قد يحتاج فنياً
  مختلفاً تماماً)، idempotent عبر فحص `corrective_work_order` قبل الإنشاء.

**اختبار:** سجِّل بند فحص بنتيجة "Fail" بدون صورة ← تأكد من رفض الحفظ.
أضف الصورة واحفظ ← تأكد من إنشاء Asset Work Order تلقائياً بأولوية
"حرج" مرتبط بالبند. احفظ المستند مرة أخرى بلا تغيير ← تأكد من **عدم**
إنشاء أمر عمل مكرر.

---

*(البند الأخير من القسم ج — ESG/الكربون وشذوذ استهلاك الطاقة — يُضاف
هنا.)*
