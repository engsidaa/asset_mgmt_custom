# مزايا احترافية إضافية — الجولة الثانية

بعد إتمام منصة الـ EAM/CMMS المؤسسية (Phase 6)، طُلب اقتراح مزايا احترافية
إضافية. من 7 مزايا مقترحة، وافق المستخدم على تنفيذ 6 منها (تم تخطي #2 —
تصدير الضريبة/VAT الكويتية — بطلب صريح لحين فهمها لاحقاً).

كل ميزة أدناه اتبعت نفس انضباط الجلسة: تحقّق من سلوك Core الحقيقي أولاً،
تحقّق من عدم وجود منطق مشابه بالفعل، تنفيذ الفجوة الفعلية فقط، تحقّق
تركيبي (JSON/Python/JS)، ثم commit + push فوري.

---

## 1. لوحة تحكم تنفيذية (Executive BI Dashboard)

**المشكلة:** لا توجد رؤية مجمّعة سريعة (Group By/اتجاهات زمنية) بدون فتح
تقرير كامل في كل مرة.

**الحل:** 7 سجلات `Dashboard Chart` قياسية (`fixtures/dashboard_chart.json`)
تستخدم الآلية الأصلية في Frappe core بالكامل (Group By / Count Timeseries)
بلا أي كود رسم مخصص:
- عدد الأصول حسب الفئة / حسب الفرع
- متوسط مؤشر صحة الأصل (AHI) حسب الفئة
- متوسط التكلفة السنوية للملكية حسب الفئة
- أوامر العمل حسب الحالة / حسب الأولوية
- اتجاه أوامر العمل الشهرية (آخر سنة)

4 مخططات مرتبطة بـ workspace "إدارة الأصول" و3 بـ"الصيانة" — كتلة
`content` من نوع `chart` **مع** صف مطابق في جدول `charts` الفرعي لكل
مخطط (نفس الدرس المستفاد من خطأ `number_cards` السابق في هذه الجلسة).

---

## 2. تفويض مؤقت لصلاحية الاعتماد (Approval Delegation)

**المشكلة:** مصفوفة اعتماد "طلب أصل" (Asset Requisition) الثلاثية
المراحل لا تدعم غياب المعتمد (إجازة/مهمة خارجية) — لا يوجد آلية Frappe
Workflow قياسية هنا أصلاً لأن المرحلة الثانية تشترط "مستخدم محدد" (مدير
فرع)، وليس دوراً عاماً.

**الحل:** DocType جديد `Asset Approval Delegate` (تفويض دور وظيفي محدد،
أو تفويض بدلاً من مدير فرع محدد، بفترة من/إلى تاريخ) + طبقة تحقق مشتركة
`approvals.py` (`is_delegated_for_role` / `is_delegated_for_branch_manager`)
مُستدعاة من 3 نقاط فحص في `asset_requisition.py` — بلا أي تعديل لإعداد
الفرع/الدور نفسه.

---

## 3. مسح جماعي بالباركود للتدقيق المادي

**المشكلة:** جرد الأصول ميدانياً (Asset Physical Audit) يتطلب فتح
المستند وتحديث كل صف يدوياً — بطيء لمئات الأصول.

**الحل:** صفحة Desk "مسح الجرد المادي بالباركود" (اختيار جرد قائم أو
إنشاء جرد جديد لفرع، ثم حقل مسح واحد بحلقة Enter) + دالة جديدة
`Asset Physical Audit.scan_mark_found(identifier)` تُعلِّم كل أصل ممسوح
"موجود" فوراً (أو تُضيفه كسطر جديد إن كان أصلاً غير متوقَّع بهذا الموقع).
استُخرجت `resolve_asset_identifier()` من `api/v1/mobile.py.scan_asset`
لتصبح نقطة مطابقة كود مشتركة وحيدة في التطبيق.

---

## 4. إرسال Twilio WhatsApp/SMS فعلي

**المشكلة:** التنبيهات الحرجة كانت تعمل فقط عبر Webhook عام يتطلب أتمتة
خارجية (Zapier/Make/n8n) لإرسال الرسالة الفعلية.

**الحل:** حقول Twilio جديدة في Asset Mgmt Settings (Account SID، Auth
Token مُشفَّر، رقم مرسل SMS، رقم مرسل واتساب) + `_send_via_twilio()` في
`notifications.py` تنادي Twilio Messages API REST مباشرة (بدون SDK) —
**مستقلة تماماً** عن الـ Webhook الموجود (يمكن تفعيل أحدهما أو كليهما).

**يتطلب من المستخدم:** فتح حساب Twilio فعلي، وتعبئة SID/Token/الأرقام في
Asset Mgmt Settings، وتفعيل "استخدام Twilio" — لن يعمل بدون ذلك.

---

## 5. سجل تدقيق كامل للأصل (Full Audit Trail)

**المشكلة:** تاريخ الأصل الكامل (نشاط، تعليقات، أوامر عمل، إصلاحات،
فحوصات سلامة، تحليلات أعطال، مطالبات ضمان، شهادات امتثال) موزّع على 8
مستندات مختلفة — لا يوجد عرض واحد لتدقيق داخلي/خارجي أو تقييم قبل بيع.

**الحل:** `audit_trail.py` (`get_asset_audit_trail`) يجمع كل المصادر
الثمانية في سجل زمني واحد + Print Format جديد "Asset Full Audit Trail"
(متاح من قائمة طباعة نموذج الأصل مباشرة، وقابل للتصدير PDF).

---

## 6. أداة مقارنة سيناريوهات الإهلاك

**المشكلة:** لا توجد طريقة لتجربة "ماذا لو استخدمنا قسط متناقص بدل قسط
ثابت" أو "ماذا لو غيّرنا العمر الإنتاجي" دون تعديل جدول الإهلاك الفعلي.

**الحل:** `depreciation_scenarios.py` (`compare_scenarios`) يبني
`Asset Depreciation Schedule` واحد **في الذاكرة فقط** (بلا `.save()`
إطلاقاً) ويُعيد استخدام محرك حساب الإهلاك الأصلي في ERPNext core حرفياً
— بدل إعادة كتابة معادلات القسط الثابت/المتناقص يدوياً. صفحة Desk جديدة
"مقارنة سيناريوهات الإهلاك" تعرض سيناريوهين (أ/ب) جنباً إلى جنب: إجمالي
الإهلاك، القيمة الدفترية النهائية، وجدولا الإهلاك التفصيليان.

---

## بعد التنفيذ (على السيرفر الفعلي)

```
git pull && bench migrate && bench clear-cache && bench build && bench restart
```

كل الاختصارات الجديدة مرتبطة في workspace "إدارة الأصول" (لوحة BI،
تفويض الاعتماد، مسح الباركود، مقارنة الإهلاك) — لا حاجة لأي إعداد يدوي
إضافي عدا **تفعيل Twilio يدوياً** إن أردت استخدامه فعلياً (بند 4 أعلاه).
