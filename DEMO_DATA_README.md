# دليل تشغيل البيانات التجريبية (Demo Data) عبر المتصفح

هذا الملف موجَّه لأي شخص (أو AI Agent له وصول لمتصفح حقيقي) هيدخل على النظام
فعلياً من الواجهة (Desk UI) عشان يضيف بيانات تجريبية لكل وحدات تطبيق إدارة
الأصول، ينفّذها، يتأكد إنها اشتغلت صح، ويشرح النتيجة — بدون كتابة أي كود
جديد. كل المطلوب موجود بالفعل داخل النظام كصفحة واحدة.

**لماذا الملف ده موجود:** الأداة اتبنت واتختبرت هذا الجلسة عن طريق تشغيلها
فعلياً على السيرفر ومراجعة نتائجها سطراً سطراً — مش بس مراجعة كود. لو AI تاني
هيكمل التجربة، الملف ده بيدّيله كل السياق اللي محتاجه بدل ما يبدأ من الصفر.

---

## 1) قبل ما تبدأ — إعداد أساسي مرة واحدة

الأداة محتاجة حسابات محاسبية حقيقية مربوطة بفئة الأصل الأساسية قبل ما تشتغل
بالكامل (من غيرها هتنجح أغلب الخطوات وتتخطى بس خطوات الإهلاك والإصلاح
المحاسبي). شغّل السكريبت ده **مرة واحدة** (آمن يتكرر لو حصل):

```
cd ~/frappe-bench
bench --site e-u40.hosetia.com execute asset_mgmt_custom.setup.bootstrap_demo_master_data.run
```

بيضبط تلقائياً: حساب الأصل الثابت، حساب الوساطة (CWIP)، حساب مصروف الصيانة،
حساب التزامات الصيانة المستحقة، حساب مجمع الإهلاك، حساب مصروف الإهلاك —
ويربطهم كلهم بفئة أصل واحدة اسمها **"أصول عامة"**.

بعد أي `git pull` لتحديثات جديدة في الكود:

```
bench --site e-u40.hosetia.com migrate
bench --site e-u40.hosetia.com restart
```

---

## 2) افتح صفحة المعالج من المتصفح

```
https://e-u40.hosetia.com/app/asset-demo-wizard
```

الصفحة اسمها **"معالج البيانات التجريبية"**، ومتاحة فقط لمستخدم بدور
System Manager. لو المستخدم مسجّل دخول بدور تاني هتظهر رسالة رفض.

الصفحة فيها 4 أزرار رئيسية أعلى الصفحة، وقائمة خطوات مقسّمة لمجموعات في
النص، وسجل تنفيذ (Console) أسود تحت.

---

## 3) الأزرار الأربعة — ووظيفة كل واحد

| الزر | بيعمل إيه |
|---|---|
| **تشغيل كل الخطوات** | يشغّل كل الخطوات (17 خطوة) بالترتيب تلقائياً، وفي الآخر يعرض تقرير التغطية تلقائياً |
| **تقرير التغطية** | يقارن كل نوع مستند في التطبيق (52 نوعاً) بما تم اختباره فعلياً في هذا التشغيل، ويوضح أي نوع لم يُختبر إطلاقاً أو اتُختبر بسيناريو واحد بس |
| **تشغيل كل التقارير** | يفتح كل تقرير (37 تقريراً) في التطبيق فعلياً بنفس الطريقة اللي المتصفح بيفتحها بيها، ويتأكد إنه ما بيطلعش خطأ |
| **حذف كل البيانات التجريبية** (أحمر) | يحذف كل مستند أنشأته الأداة، بالترتيب العكسي الآمن، بدون المساس بأي بيانات حقيقية أخرى |

كل خطوة كمان ليها زر "تشغيل" منفصل بجانبها لو حابب تجربها لوحدها.

### الترتيب المقترح للتجربة الكاملة

1. دوس **"تشغيل كل الخطوات"** — استنى لحد ما يظهر "انتهى تشغيل كل الخطوات"
   وبعدها تقرير التغطية تلقائياً.
2. دوس **"تشغيل كل التقارير"** — يتحقق من كل تقرير في التطبيق ضد البيانات
   اللي اتعملت للتو.
3. (اختياري) افتح Workspace **"إدارة الأصول"** من القائمة الجانبية وشوف
   لوحة المؤشرات (KPIs) — المفروض تبقى بأرقام حقيقية غير صفرية.
4. لما تخلص تجربة، دوس **"حذف كل البيانات التجريبية"** لو عايز ترجع النظام
   لحالته الأصلية.

---

## 4) الـ 17 خطوة بالتفصيل — مين بيعمل إيه

الخطوات مقسّمة على 6 مجموعات، وكل خطوة بتنشئ بيانات حقيقية (مش وهمية بمعنى
مؤقتة — بتفضل موجودة في قاعدة البيانات لحد ما تحذفها بنفسك):

### الأساسيات
1. **تجهيز البيانات الأساسية وأصل تجريبي** — يجهّز شركة/فئة أصل/صنف/موقع/فرع
   (لو مش موجودين) وينشئ أول أصل تجريبي معتمَد.
2. **سلسلة اعتماد طلب استحداث أصل** — طلب أصل جديد يمشي في سلسلة اعتماد
   3 مراحل (المالية ← مدير الفرع ← إدارة الأصول) لحد ما يبقى Approved،
   + طلب ثانٍ يتُرفض في نص السلسلة (اختبار مسار الرفض).
3. **الترميز (Coding) والتفعيل التشغيلي** — أصل جديد يتترمّز بباركود
   (Sticker Code) ثم يتفعّل تشغيلياً، + أصل ثانٍ بمسار "Iron Code" (نقش
   حديدي) بدل الباركود.

### الحركة والنقل
4. **نقل أصل بين الفروع + تأكيد الاستلام** — سند نقل (Transfer) بين موقعين،
   الأصل يتحط "In Transit" ثم يترجع "Operational" بعد تأكيد الاستلام.
5. **تنفيذ طلب معتمَد** — سيناريوهين: (أ) أصل احتياطي جاهز فيتكتشف تلقائياً
   ويتسلَّم عبر سند Receipt، (ب) صنف مش متوفر فيتحوَّل الطلب لطلب شراء.
6. **إعارة أصل واسترجاع العهدة** — أصل يتعار لموظف، العهدة تنتقل له، وبعدين
   يترجع والعهدة ترجع لصاحبها الأصلي.
7. **تسليم/استلام عهدة فرع (Handover)** — محضر تسليم رسمي بين مدير خارج
   ومدير وارد لأصول فرع كامل.

### المالية والمحاسبة
8. **القيد المحاسبي لإصلاح أصل (OpEx / CapEx)** — 3 سيناريوهات: إصلاح عادي
   (قيد يومية على حساب مصروف)، رسملة صحيحة (قيد على حساب وساطة + تمديد عمر)،
   ومحاولة رسملة **بدون** تمديد عمر يجب أن تُرفض (اختبار سلبي).
9. **طلب احتفاظ بعهدة** — أصل بعهدة موظف، وطلب يسمح باستثنائه من إجراءات
   إخلاء الطرف القياسية في HRMS.
10. **شطب أصل والقيد المحاسبي** — طلب شطب يُعتمد وينشئ قيد يومية بالخسارة،
    + طلب ثانٍ يُرفض (اختبار سلبي).
11. **طلب وتنفيذ التخلص من أصل** — طلب تخلص يُعتمد ويُنفَّذ، + طلب ثانٍ يُرفض.
12. **احتساب الإهلاك الدوري (Straight Line)** — أصل بقيمة 24,000 على 12
    قسط شهري (2,000 لكل قسط)، يتأكد فعلياً إن جدول الإهلاك اتعمل صح.
    ⚠️ يتخطى (Skip) لو حسابات الإهلاك مش مضبوطة بعد — راجع القسم 1.

### الفحص والسلامة والامتثال
13. فحص سلامة (ناجح + راسب)، تقييم مخاطر، تصريح عمل، شهادة امتثال، ترخيص،
    سجل معايرة، تقييم حالة، تصنيف أهمية حرجة، بلاغ حادثة، شكوى.

### الصيانة والأعمال التشغيلية
14. أمر شغل، سجل صيانة، عقد صيانة مرتبط بالأصل، موازنة صيانة، طلب قطعة
    غيار، جدول تنظيف، تحليل عطل، خطة إحلال، طلب تمديد عمر.

### المراقبة والسجلات التشغيلية
15. قراءة عداد، سجل استخدام، سجل وقود، سجل طاقة، سجل بيئي، تقييم أداء،
    مكوّن أصل، سجل تنقّل.

### الإدارة المالية والتعاقدات
16. موازنة رأسمالية (+ اختبار سلبي: تجاوز الميزانية الفرعية للإجمالي يُرفض)،
    عقد مورد، تقييم أداء مورد، تجديد تأمين، عقد إيجار، ترخيص برمجي، مطالبة
    ضمان، أرشفة مستندات.

### دورة حياة الأصل والعهدة
17. حجز أصل (+ اختبار سلبي: حجز متعارض على نفس الفترة يُرفض)، تسليم مؤقت،
    تخصيص لموظف، طلب إعادة، طلب نقل بين فروع، سجل تدريب، شهادة تخلص،
    مستهدف أداء، جرد فعلي (بند "موجود" + بند "مفقود").

### شبكة أمان
18. **إنشاء سجل تجريبي واحد لكل وحدة فرعية متبقية** — أي نوع مستند في
    التطبيق لم تغطّه الخطوات أعلاه (نادراً ما يتبقى شيء) يُنشأ له سجل
    تلقائي بحقول مُولَّدة حسب نوع كل حقل.

---

## 5) قراءة سجل التنفيذ (Console)

كل سطر في السجل يبدأ برمز:

- ✅ **نجح** — تم إنشاء/اعتماد المستند بنجاح.
- ⏭️ **تخطي** — الخطوة اتخطّت لسبب واضح (زي حسابات إهلاك غير مُعرَّفة)، مش
  عطل.
- ℹ️ **معلومة** — رسالة توضيحية، مش نتيجة اختبار.
- ❌ **فشل** — عطل حقيقي محتاج مراجعة.

**مهم:** بعض الأسطر اللي فيها ✅ بتقول "تم الرفض بشكل صحيح" أو "تم الرفض
بنجاح" — دي **سيناريوهات سلبية متعمَّدة** (بنتأكد إن النظام بيرفض حاجة غلط
صح)، مش أعطال. النجاح هنا معناه "النظام رفض زي ما هو متوقع".

---

## 6) تقرير التغطية — إزاي تقرأه

- **"X من 52 نوع مستند تم اختباره"** — لو X = 52 يبقى كل نوع مستند في
  التطبيق اتلمس مرة على الأقل هذا التشغيل.
- **قائمة "لم يتم اختبار... إطلاقاً"** — لو ظهرت، يبقى فيه نوع مستند لسه
  مالوش أي سيناريو، محتاج مراجعة كود.
- **قائمة "بسيناريو واحد فقط"** — دي مش عطل، بس إشارة إن النوع ده اتجرب
  بشكل واحد بس (مش نجاح+فشل مثلاً)؛ لو محتاج تغطية أعمق لنوع معين قوله
  بالاسم.

---

## 7) تشغيل كل التقارير — الـ 37 تقرير

بيفتح كل تقرير بفلاتر افتراضية معقولة (شركة/فرع/مركز تكلفة/فئة أصل/سنة
مالية/فترة سنة كاملة) — مش فاضية تماماً، عشان التقارير اللي محتاجة فلتر
إلزامي (زي "Branch Asset Handover") تتفحص فعلياً برضو. كل سطر بيوريك اسم
التقرير وعدد الصفوف اللي رجعها، أو رسالة الخطأ بالظبط لو فشل.

الأقسام اللي التقارير موزّعة عليها في الـ Workspace (لمرجعية سريعة):
تقارير التشغيل، التقارير المالية، التقارير المتقدمة — الإجمالي 37 تقريراً.

---

## 8) لوحة المؤشرات (KPIs) في Workspace "إدارة الأصول"

بعد "تشغيل كل الخطوات"، المفروض تشوف أرقام غير صفرية زي:
Total Active Assets، أصول في العبور، Open Incidents، Active Bookings، إلخ.
لو كل الأرقام صفر بعد التشغيل، يبقى إما البيانات اتحذفت (زرار الحذف)، أو
التشغيل فشل من البداية — ارجع لسجل الـ Console.

---

## 9) حذف البيانات التجريبية — إزاي شغّالة بأمان

كل مستند تنشئه الأداة (بما فيه المستندات الجانبية اللي بتتعمل تلقائياً زي
سند الاستلام الأول لأي أصل، أو القيد اليومي لإصلاح/شطب) بيتسجّل في جدول
داخلي اسمه **"Asset Demo Run Log"**. زر الحذف بيمشي على السجل ده بالترتيب
العكسي (الأحدث أولاً)، يلغي اعتماد أي مستند submitted، ثم يحذفه نهائياً —
وبما إن الترتيب عكسي، أي مستند بيشير لمستند تاني اتعمل بعده بيتحذف الأول.
**لا يمس أي بيانات حقيقية خارج ما أنشأته الأداة نفسها.**

---

## 10) ملفات الكود ذات الصلة (لو محتاج تعدّل)

- `asset_mgmt_custom/setup/demo_wizard.py` — كل منطق الخطوات + نقاط
  الوصول (`get_steps`, `run_step`, `coverage_report`, `run_all_reports`,
  `cleanup_all`).
- `asset_mgmt_custom/asset_mgmt_custom/page/asset_demo_wizard/` — واجهة
  الصفحة (JSON + JS).
- `asset_mgmt_custom/asset_mgmt_custom/doctype/asset_demo_run_log/` —
  الدوكتايب اللي بيتتبَّع كل مستند تجريبي.
- `asset_mgmt_custom/setup/bootstrap_demo_master_data.py` — إعداد
  الحسابات المحاسبية الأساسي (القسم 1 أعلاه).

كل الأزرار والخطوات مبنية كـ `@frappe.whitelist()` methods بايثون عادية —
أي AI أو سكريبت تاني يقدر يناديها مباشرة برضو عبر `frappe.call` من
الـ JS console في المتصفح، أو حتى عبر `bench execute` لو محتاج، بدون
المرور بالواجهة خالص.
